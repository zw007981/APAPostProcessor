# 核心接口契约（冻结清单）

> 本文件登记项目中"一旦确定不可随意更改"的核心抽象接口。每一条只记录**设计意图 + 指向实际头文件的链接**，
> 具体签名以链接的头文件为唯一真值，**禁止在本文件里复制代码片段**，避免和头文件不同步。

## 使用方式

- Dev Agent 在实现/修改核心接口前，必须先读本文件确认该接口是否已冻结。
- Review Agent 审查"需求对齐"维度时，以本文件登记的接口为基准之一。
- 新增/变更已冻结接口，必须先在下方"变更记录"登记原因，再动手改代码；未登记的接口签名变更视为违反 [.agents/rules.md](../.agents/rules.md) 红线。

## 已冻结接口清单

| 接口 | 设计意图（一句话） | 头文件链接 | 状态 |
|---|---|---|---|
| `Position` | 纯二维坐标，用于栅格原点/中心等只需位置的场景 | [src/util/position.h](../src/util/position.h) | 已冻结 |
| `Pose` | 纯几何位姿（位置+朝向），**不携带任何路径规划派生量**，用于车辆瞬时位姿等通用场景 | [src/util/pose.h](../src/util/pose.h) | 已冻结 |
| `PathPoint` | `Pose` + 一组"不一定存在"的派生量（曲率 `kappa`、状态量 `v`/`delta`、控制量 `a`/`delta_dot`），代表"路径规划/轨迹优化输出的轨迹点"；这些派生量默认值均为 NaN（代表未提供），只能通过 `hasXxx()`/`getXxx()`/`setXxx()` 三件套访问，`getXxx()` 在未设置时抛出 `std::logic_error`；proto 层不携带这些派生量（详见下方 proto 兼容性说明） | [src/util/trajectory_point.h](../src/util/trajectory_point.h) | 已冻结 |
| `Maneuver` | 同一运动方向下的 `PathPoint` 有序序列，`Path` 的最小分段单元 | [src/util/maneuver.h](../src/util/maneuver.h) | 已冻结 |
| `Path` | 由 `Maneuver` 序列组成的路径，封装逐点追加时的去重/插值/方向推断，曲率估计在 `finalize()` 中统一批量完成 | [src/util/path.h](../src/util/path.h) | 已冻结 |
| `VehicleParams` | 车辆物理参数（长宽轴距/最大转向角/纵向加减速极限/最大转向角速度），派生最大曲率 `max_kappa` | [src/vehicle/vehicle_params.h](../src/vehicle/vehicle_params.h) | 已冻结 |
| `VehicleFootprintModel` | 用内/外两组圆的查找表近似车身占据区域，按航向角离散化 | [src/vehicle/vehicle_footprint_model.h](../src/vehicle/vehicle_footprint_model.h) | 已冻结 |
| `GridMap` | 离散占据栅格，提供物理坐标与栅格索引的双向映射 | [src/spatial/grid_map.h](../src/spatial/grid_map.h) | 已冻结 |
| `ESDFMap` | 基于 `GridMap` 构建的欧式符号距离场，提供双线性插值的距离与梯度查询 | [src/spatial/esdf_map.h](../src/spatial/esdf_map.h) | 已冻结 |
| `ApaEsdfMapAdapter` | 把 `ESDFMap` 适配为 `third_party/StcSQP` 的 `stc_SQP::EsdfMapInterface`，衔接业务地图与通用求解引擎 | [src/core/NMPC/apa_esdf_map_adapter.h](../src/core/NMPC/apa_esdf_map_adapter.h) | 已冻结 |
| `vehicle_circle_geometry::ExtractLocalCircleCenters` | 从 `VehicleFootprintModel` 提取车身坐标系下的圆心局部坐标，供 `CircleFootprintEsdfConstraint` 使用 | [src/core/NMPC/vehicle_circle_geometry.h](../src/core/NMPC/vehicle_circle_geometry.h) | 已冻结 |
| `PathToOcpConverter` | 把 `Path` 转换为 `MultiStageOCP` 描述与初始猜测轨迹；已拆分为 `computeSegmentProfiles()` / `generateInitialGuess()` / `buildOcp()` 三个独立入口，`convert()` 保持向后兼容 | [src/core/NMPC/path_to_ocp_converter.h](../src/core/NMPC/path_to_ocp_converter.h) | 已冻结 |
| `SegmentProfile` | 单段 OCP 离散化描述（N/dt/dt_array/v_sign/is_terminal），衔接 `PathToOcpConverter` 的 OCP 装配与初始猜测生成 | [src/core/NMPC/path_to_ocp_converter.h](../src/core/NMPC/path_to_ocp_converter.h) | 已冻结 |
| `NmpcSolver` | 编排 Path→OCP 转换、StcSQP 求解与求解后机动段裁剪，返回优化轨迹与分段信息；新增 `optimize(MultiStageOCP, Trajectory, ESDFMap)` 扩展点，供预装配 OCP/初始猜测的场景直接接入 | [src/core/NMPC/nmpc_solver.h](../src/core/NMPC/nmpc_solver.h) | 已冻结 |
| `BSplineSmoother` | 对单个 `Maneuver` 拟合四次 B 样条曲线，输出控制点、弧长重参数化表与密集配点缓存；输入 `Maneuver` + `ESDFMap` + `VehicleFootprintModel`，构造函数与 `smooth()` 为唯一外部入口 | [src/preprocessing/bspline_smoother.h](../src/preprocessing/bspline_smoother.h) | 已冻结 |
| `BSplineSmootherConfig` | `BSplineSmoother` 的配置结构体，字段默认值与来源见 `docs/default_params.md` | [src/preprocessing/bspline_smoother.h](../src/preprocessing/bspline_smoother.h) | 已冻结 |
| `SpeedProfilePlanner` | 基于空间域 $v^2$ 凸优化的纵向速度规划器：输入密集配点（`s`/`kappa`/`min_esdf_dist`）、方向符号与可选尖点，输出带符号的速度/加速度/时间戳 | [src/preprocessing/speed_profile_planner.h](../src/preprocessing/speed_profile_planner.h) | 已冻结 |
| `SpeedProfilePlannerConfig` | `SpeedProfilePlanner` 的配置结构体，字段默认值与来源见 `docs/default_params.md` | [src/preprocessing/speed_profile_planner.h](../src/preprocessing/speed_profile_planner.h) | 已冻结 |
| `SpeedProfileInput` | `SpeedProfilePlanner` 的输入结构体，每个密集配点对应弧长、曲率与最小 ESDF 距离 | [src/preprocessing/speed_profile_planner.h](../src/preprocessing/speed_profile_planner.h) | 已冻结 |
| `SpeedProfileResult` | `SpeedProfilePlanner` 的输出结构体，包含求解状态、带符号速度/加速度/时间戳与原始 QP 变量 $b=v^2$ | [src/preprocessing/speed_profile_planner.h](../src/preprocessing/speed_profile_planner.h) | 已冻结 |
| `DifferentialFlatnessSolver` | 基于微分平坦性质，由 B 样条几何导数与速度剖面解析反推前轮偏角 `delta` 及其变化率 `delta_dot`，输出完整 NMPC 状态/控制序列 | [src/preprocessing/differential_flatness_solver.h](../src/preprocessing/differential_flatness_solver.h) | 已冻结 |
| `DifferentialFlatnessSolverConfig` | `DifferentialFlatnessSolver` 的配置结构体，字段默认值见 `src/preprocessing/differential_flatness_solver.h` | [src/preprocessing/differential_flatness_solver.h](../src/preprocessing/differential_flatness_solver.h) | 已冻结 |
| `DifferentialFlatnessInput` | `DifferentialFlatnessSolver` 的输入结构体，每个密集配点对应 B 样条几何导数与速度规划结果 | [src/preprocessing/differential_flatness_solver.h](../src/preprocessing/differential_flatness_solver.h) | 已冻结 |
| `DifferentialFlatnessResult` | `DifferentialFlatnessSolver` 的输出结构体，包含完整 `PathPoint` 状态/控制序列 | [src/preprocessing/differential_flatness_solver.h](../src/preprocessing/differential_flatness_solver.h) | 已冻结 |
| `AdaptiveResamplerConfig` | `AdaptiveResampler` 的配置结构体，字段默认值见 `src/preprocessing/adaptive_resampler.h` | [src/preprocessing/adaptive_resampler.h](../src/preprocessing/adaptive_resampler.h) | 已冻结 |
| `AdaptiveResamplerSegmentInput` | `AdaptiveResampler` 的单段输入结构体，聚合 `BSplineSmoother::DensePointData` 与 `DifferentialFlatnessSolver` 产出的完整状态/控制序列 | [src/preprocessing/adaptive_resampler.h](../src/preprocessing/adaptive_resampler.h) | 已冻结 |
| `AdaptiveResamplerResult` | `AdaptiveResampler` 的输出结构体，包含固定维数打靶点序列与对应 `delta_t` 静态参数向量 | [src/preprocessing/adaptive_resampler.h](../src/preprocessing/adaptive_resampler.h) | 已冻结 |
| `AdaptiveResampler` | 轨迹自适应重采样与维数固化器：将预处理管线前序阶段（B 样条平滑/速度规划/微分平坦解算）产出的密集序列压缩至 NMPC 固定维数，并在弧长域处理完成后注入换挡/起始转向对齐补丁 | [src/preprocessing/adaptive_resampler.h](../src/preprocessing/adaptive_resampler.h) | 已冻结 |
| `StaticCorridorBuilderConfig` | `StaticCorridorBuilder` 的配置结构体，字段 `hard_margin`/`soft_margin` 默认值来源见 `docs/default_params.md` | [src/preprocessing/static_corridor_builder.h](../src/preprocessing/static_corridor_builder.h) | 已冻结 |
| `StaticCorridorBuilder` | 静态安全走廊构建器：以自适应重采样阶段产出的固定维数 Z_ref 为泰勒展开基准点，一次性查询 ESDF 距离与梯度，输出标准线性不等式 `C_matrix`/`d_vector`（含 hard/soft 双边界） | [src/preprocessing/static_corridor_builder.h](../src/preprocessing/static_corridor_builder.h) | 已冻结 |
| `PreprocessingPipelineConfig` | 预处理管线配置，聚合五个预处理阶段的 Config 结构体 + `use_static_corridor` 过渡开关 + `collision_safety_margin` 跨阶段统一数值容差（默认 `EPSILON_PRECISE` = 1e-6，纯浮点误差兜底；外圆自身提供安全缓冲，不叠加物理安全裕度）+ `enable_debug_output` 调试数据透传开关（默认 `false`） | [src/preprocessing/preprocessing_pipeline.h](../src/preprocessing/preprocessing_pipeline.h) | 已冻结 |
| `PreprocessingPipeline` | 预处理管线组装器：按 Maneuver 顺序依次调用 B 样条平滑、速度规划、微分平坦解算、自适应重采样、静态走廊构建五个阶段，输出固定维数 Z_ref/U_ref、delta_t 序列与静态走廊系数；`enable_debug_output` 开启时透传各阶段中间产物 | [src/preprocessing/preprocessing_pipeline.h](../src/preprocessing/preprocessing_pipeline.h) | 已冻结 |
| `PreprocessingPipelineResult` | `PreprocessingPipeline::run()` 的输出结构体，包含 Z_ref、delta_t、C_matrix/d_vector、各阶段耗时分解、`debug_maneuver_outputs`（仅在 `enable_debug_output=true` 时填充）、`original_z_ref`（Subplot 1 原始路径对比）、`hard_margin_used`/`soft_margin_used`/`outer_row_num_used`（可视化侧与管线参数保持一致） | [src/preprocessing/preprocessing_pipeline.h](../src/preprocessing/preprocessing_pipeline.h) | 已冻结 |
| `PreprocessingToOcpConverter` | 把 `PreprocessingPipelineResult`（`z_ref`/`delta_t`）转换为可直接喂给 `NmpcSolver` 的 `MultiStageOCP` + `Trajectory` Warm Start；同步按 OCP 总步数截断 `c_matrix`/`d_vector` | [src/core/NMPC/preprocessing_to_ocp_converter.h](../src/core/NMPC/preprocessing_to_ocp_converter.h) | 已完成 |
| `DetailSeriesData` | `Visualizer` 的 Details 图序列缓存结构体：`label`/`color`/`line_width` 绘图样式 + `index`/`x`/`y`/`curvature`/`heading`/`ref_s`/`v`/`a`/`delta`/`delta_dot` 信号序列 + `shift_indices` 换挡点标记 | [src/util/visualizer.hpp](../src/util/visualizer.hpp) | 已冻结 |
| `Visualizer` | 可视化器：三类画布统一入口 `save()`——环境场景画布（`plotGridMap`/`plotPath`/`plotVehicle`/`focusOnVehicle` 世界坐标系绘制 + 统一栅格）、Details 2x2 子图画布（`plotPathDetails`）、轨迹对比画布（`plotTrajectory`，左栏空间几何 + 右栏信号垂直堆叠）；坐标刻度为量级解耦的 Wilkinson 优美数算法（1/2/2.5/5/10 五档），堆叠子图刻度密度按绘图区像素高度自适应 | [src/util/visualizer.hpp](../src/util/visualizer.hpp) | 已冻结 |
| `Trajectory` / `TrajectoryValidationConfig` / `TrajectoryValidationResult` | 带时间戳与完整运动学状态/控制量的 `TrajectoryPoint` 序列；`validate()` 三项校验：碰撞安全（多圆覆盖 + ESDF）、终点收敛（位置/航向误差）、运动学可行性（梯形配点残差：对相邻两点检验 ẋ=v·cosθ、θ̇=v·tanδ/L、v̇=a、δ̇=δdot 在梯形积分意义下的残差，Δt 超过 `max_kinematic_dt` 的点对因 O(Δt³) 截断主导而跳过，时间戳缺失/量未齐备的点对不证伪）；轴距取 `VehicleFootprintModel::getWheelbase()`，不依赖任何求解器内部模型 | [src/util/trajectory.h](../src/util/trajectory.h) | 已冻结 |
| `TimeProfileConfig` / `TimeProfile` / `ComputeTimeProfile` | 原始几何路径的时间参数化（唯一时间律）："最快走完"前提的 bang-bang 梯形加减速——输入累计弧长/运动方向签名曲率/方向符号/换挡点下标四序列与车辆纵向极限，输出 v/a/t 三序列；首末点/换挡点零速，前向-后向扫描受极限速度与曲率侧向加速度封顶，空间加速度 σ·a ∈ [max_decel, max_accel] 的 box 约定与 `SpeedProfilePlanner` 一致；输入长度不一致/换挡点越界/配置或纵向极限非法抛 `std::invalid_argument` | [src/util/time_profile.h](../src/util/time_profile.h) | 已冻结 |
| `BlockTridiagonalSolver` | 固定 6x6（h=3）块的块 Thomas 求解器：O(M) 块 LU 前向消元+回代，消元时对每个对角块同时保留其自身与转置的完全主元 LU（`FullPivLU`，6x6 固定块栈上运算），`solve()`/`solveTranspose()` 共享同一分解；尺寸不匹配/空对角线抛 `std::invalid_argument`，消元后奇异块抛 `std::runtime_error`，未分解先求解抛 `std::logic_error` | [src/core/ALM/block_tridiagonal_solver.h](../src/core/ALM/block_tridiagonal_solver.h) | 已冻结 |
| `MincoBoundaryCondition` / `MincoBoundaryCondition2d` | MINCO 边界条件结构体：单维位置/速度/加速度（h=3 时 0~2 阶导数），及 θ-s 二维组合；`MincoTrajectory::setTrajectory` 的起终点输入类型 | [src/core/ALM/minco_trajectory.h](../src/core/ALM/minco_trajectory.h) | 已冻结 |
| `MincoTrajectory` | MINCO θ-s 分段 5 阶多项式轨迹（原生二维，θ 与 s 共享同一 K(T) 分解）：K(T)/b 装配（局部归一化时间 τ=t/T_i 基，块行布局保证全部主对角块非奇异；内部航点在 b 中出现两次，终点位置固定位于 b 第 6M-3（0 基）个位置）与块三对角求解、按全局/段内时刻求 0~5 阶实时间导数、τ↔T 分段光滑双射及解析导数、s_f 的 K(T)^{-T} 伴随梯度；实时间导数基函数行 `DerivativeBasisRow` 公开（纯函数，供优化模块装配代价对系数的梯度复用） | [src/core/ALM/minco_trajectory.h](../src/core/ALM/minco_trajectory.h) | 已冻结 |
| `BicycleKinematicsExtractor` | θ-s 空间到阿克曼状态的运动学映射与物理约束惩罚提取器：`extract()` 由单时刻 θ-s 采样点解析 v/a/δ/δ̇（δ 取 atan(L·θ̇·ṡ/(ṡ²+ε_g))，δ̇ 按 ε_g 分母正则化公式，值与梯度同一公式）；`steerGradients()` 给出 δ/δ̇ 对输入导数的解析梯度；`evaluatePenalties()` 给出四个防奇异二次形态约束（C_v/C_a/C_δ/C_δ̇）的三次光滑外点罚 max(0,C)³（未加权）与解析梯度；构造期校验全部物理参数，非法输入抛 `std::invalid_argument` | [src/core/ALM/bicycle_kinematics_extractor.h](../src/core/ALM/bicycle_kinematics_extractor.h) | 已冻结 |
| `ThetaSSample` / `AckermannState` / `SampleGradient` / `SteerGradients` / `ConstraintPenalty` / `PhysicalConstraintPenalties` / `BicycleKinematicsConfig` | `BicycleKinematicsExtractor` 的输入/输出/配置结构体：单时刻 θ-s 采样点（θ,θ̇,θ̈,s,ṡ,s̈）、阿克曼状态量（v,a,δ,δ̇）、输入导数四分量梯度、δ/δ̇ 梯度对、单约束惩罚信息（constraint/penalty/gradient）、四约束聚合与提取器配置 | [src/core/ALM/bicycle_kinematics_extractor.h](../src/core/ALM/bicycle_kinematics_extractor.h) | 已冻结 |
| `AlmManeuverSegmenter` | 前端 Hybrid A* 路径解析器：换挡打断直接消费 `Path` 已有的方向切分；空间等距降采样按 M=⌈L/d_seg⌉、K_step=⌊(N-1)/M⌋ 抽取 A* 锚点（末锚点强制为路径终点），产出每 Maneuver 的初始 M 段估计；朝向沿路径 2π 解缠绕保证连续，累积弧长按方向带符号跨 Maneuver 连续累积；构造期校验配置，空 Path 抛 `std::invalid_argument` | [src/core/ALM/alm_maneuver_segmenter.h](../src/core/ALM/alm_maneuver_segmenter.h) | 已冻结 |
| `AlmSegmentEstimate` / `AlmManeuverEstimate` / `AlmManeuverSegmenterConfig` | `AlmManeuverSegmenter` 的输出/配置结构体：单微观段初始估计（期望终点 `desired_position`、解缠绕朝向 `theta`、带符号累积弧长 `arc_length`、时长初值 `duration`）、单 Maneuver 估计集合（direction + start_theta + start_arc_length + segments）、分段器配置（标称段长/标称速度/标称转向角速度/时长下限） | [src/core/ALM/alm_maneuver_segmenter.h](../src/core/ALM/alm_maneuver_segmenter.h) | 已冻结 |
| `AlmEsdfPenalty` | ESDF 双重安全机制惩罚：复用 `ESDFMap`/`VehicleFootprintModel` 外圆集合，对单时刻位姿 (x,y,θ) 计算 `margin_safe`/`margin_comf` 两段三次光滑外点罚 $\Phi(C)=\max(0,C)^3$ 的混合代价 $\mathcal{I}_{obs}$ 与解析梯度（含外圆随 θ 旋转的几何链式法则）；圆心越界按保守侵入处理（正惩罚、零梯度） | [src/core/ALM/alm_esdf_penalty.h](../src/core/ALM/alm_esdf_penalty.h) | 已冻结 |
| `AlmEsdfPenaltyConfig` / `AlmEsdfPoseCost` | `AlmEsdfPenalty` 的配置/输出结构体：`margin_safe`/`margin_comf`/`weight_safe`/`weight_comf`（与 ALM.md 2.4 节一致）及单时刻评估结果 `{cost, gradient}` | [src/core/ALM/alm_esdf_penalty.h](../src/core/ALM/alm_esdf_penalty.h) | 已冻结 |
| `AlmPreprocessor` | 两阶段优化流程第一阶段：以 `AlmManeuverSegmenter` 初值估计为输入，在松收敛阈值下求解 $\mathcal{J}_{pre}$（运动学约束惩罚 + 段时长平衡 + 逐段终点跟踪，不含 ESDF/ALM 乘子），输出供主优化使用的 `MincoTrajectory` 初值；决策变量 $[2(M-1)$ 航点, $M$ 个 $\tau_i$, $s_f]$，梯度经 $K(T)^{-T}$ 伴随反传与 K(T) 行对 T 的解析依赖 | [src/core/ALM/alm_preprocessor.h](../src/core/ALM/alm_preprocessor.h) | 已冻结 |
| `AlmPreprocessorConfig` / `AlmPreprocessorProblem` / `AlmPreprocessorResult` | `AlmPreprocessor` 的配置/问题装配数据/输出结构体：松收敛阈值（`lbfgs_epsilon=1e-4`、`lbfgs_max_iterations=200`、`convergence_position_tolerance=0.1m`）、四项物理惩罚权重、段时长平衡系数、换挡点软惩罚权重、时间正则系数等 | [src/core/ALM/alm_preprocessor.h](../src/core/ALM/alm_preprocessor.h) | 已冻结 |
| `AlmSolver` | PHR-ALM 主求解器：内层 L-BFGS 优化 $(^w\sigma', \tau, s_f)$（跃度闭式 $Q/T^5$ + 运动学约束 + 段时长平衡 + 换挡软惩罚 + ESDF 双重惩罚 + ALM 终端项），外层 λ/ρ 更新与位置/朝向双指标收敛判据；首轮纯软惩罚启动后 ρ^0 自适应标定，ρ 更新支持原论文无条件递增与工程门控两种模式 | [src/core/ALM/alm_solver.h](../src/core/ALM/alm_solver.h) | 已冻结 |
| `AlmSolverConfig` / `AlmSolverProblem` / `AlmSolverResult` / `AlmMultiplierState` / `AlmCostBreakdown` / `AlmOuterIterationRecord` | `AlmSolver` 的配置/问题装配/输出/乘子状态/代价分解/逐轮记录结构体：双指标收敛阈值（位置 0.05m/朝向 1.5°）、ρ 标定与更新参数（$\rho_{min}$/$\rho_{max}$/$\epsilon_\rho$/γ/门控 κ）、跃度/物理/ESDF 权重、L-BFGS 参数（主优化 ε=1e-5、max_iter=500） | [src/core/ALM/alm_solver.h](../src/core/ALM/alm_solver.h) | 已冻结 |
| `AlmManeuverMelter` / `AlmManeuverMelterConfig` / `AlmMeltResult` / `AlmManeuverMeltInfo` / `AlmMeltClass` | 机动融化与拓扑修剪：融化判据 `detectMelting` 按 θ-s 精确量（首末段端点的 \|Δs\|/\|Δθ\|）分类 NORMAL/MELTED/PIVOT 三类（与 `topology_cleaner` 第一遍判据结构一致，仅从采样几何 + Δδ 重新标定为 θ-s 精确量 + Δθ）；拓扑修剪 `meltAndPrune` 复用 `ReconstructPath` 完成第二遍重建（剔除废段、同向合并、方向相反段绝不合并）；首尾段保护（首/末段无论判据量如何均只分类为 NORMAL：不因融化判据被剔除，也不被重分类为 PIVOT，仅允许参与同向合并）、PIVOT 段保留（仅把 `direction` 改写为 `Direction::PIVOT` 信息性标签、单独保留不参与合并，采样点 x,y,v,a,δ,δ̇ 与 NORMAL 段同等对待、不做任何改写）、输出为采样 `Path`（梯形积分还原世界坐标） | [src/core/ALM/alm_maneuver_melter.h](../src/core/ALM/alm_maneuver_melter.h) | 已冻结 |
| `SampleMincoTrajectory` / `CheckMincoSampleStructure` | "MincoTrajectory → 采样 Maneuver 序列"离散化工具（独立自由函数，纯函数无内部状态，多次调用互不干扰）：世界坐标由 ṡ·u(θ) 梯形积分逐点还原（锚定起点世界坐标、跨 Maneuver 连续累积），v/a/δ/δ̇ 由 `BicycleKinematicsExtractor` 逐点解析；半开区间采样避免段连接处重复点、全局终点补在末段；方向直接取自估计，不含任何融化/剔除判断，同一工具对预处理粗优化轨迹与主优化收敛轨迹给出可直接对比的离散化结果；`CheckMincoSampleStructure` 为配套的估计-轨迹段结构一致性校验（空估计/空 Maneuver/段数不一致抛 `std::invalid_argument`） | [src/core/ALM/alm_trajectory_sampler.h](../src/core/ALM/alm_trajectory_sampler.h) | 已冻结 |
| `ApplySteerPadding` / `AlmSteerPaddingConfig` / `AlmSteerPaddingStats` | 停驻窗口"低速蠕行打轮"合法化后处理：对 \|v\|<v_epsilon 的连续停驻窗口，净 Δθ ≤ max_freeze_dtheta 时 θ 冻结为窗口首点值（消除 pivot 旋转），δ 从窗口前最后一个驱动点的限幅值按 ≤δ̇_max 斜率向后延伸过渡直至与原 δ 剖面相交（斜率继承保持相邻过渡带 δ̇ 连续），x,y/v/a/t 保持原值；净 Δθ 超阈值的真实 pivot 旋转窗口保持原样（阿克曼车辆无原地转向能力，需多点掉头）；`PostProcessor::optimizeAlm` 在机动融化后、质量门前调用 | [src/core/ALM/alm_steer_padding.h](../src/core/ALM/alm_steer_padding.h) | 已冻结 |
| `PostProcessor` | 后处理完整链路编排器，承载三条并列算法路径：`optimize()` 走 NMPC 路径（Path → PreprocessingPipeline → NmpcSolver），`optimizeAlm()` 走 ALM 路径（Path → AlmManeuverSegmenter → AlmPreprocessor → AlmSolver → AlmManeuverMelter），`optimizeDdp()` 走 DDP 路径（Path → DdpReferenceBuilder → ApaDdpSolver 阶段一 → DdpPostStage 后处理/阶段二门控精化 → 校验/回退）；三条路径互不干扰，共用同一碰撞质量门（0.02m）；ALM 路径无预处理轨迹兜底（预处理粗优化不含 ESDF 惩罚，不具备碰撞安全保证），质量门不满足时显式返回失败；DDP 路径的后处理自带回退原始 A* 路径出口（绝不输出半成品轨迹），触发回退时显式返回失败并附结构化诊断 | [src/core/post_processor.h](../src/core/post_processor.h) | 已冻结 |
| `AlmConfig` | ALM 路径配置结构体：聚合分段/预处理粗优化/PHR-ALM 主求解/ESDF 双重惩罚/机动融化五个阶段的配置 + 纵向速度上限（`VehicleParams` 未承载的规划量，默认 2.0 m/s）；继承通用 `Config` 基类（与 `NMPCConfig` 模式一致，供场景层统一管理配置）；运动学配置（轴距/转角/转角速度/加速度上限）由 `PostProcessor::DeriveKinematicsConfig` 按 `VehicleParams` 派生，不在此重复配置 | [src/core/post_processor.h](../src/core/post_processor.h) | 已冻结 |
| `DdpConfig` | DDP 路径配置结构体：聚合参考构建（`DdpReferenceBuilderConfig`）/求解编排（`ApaDdpSolverConfig`，含内层 MS-iLQR/外层 AL/代价三层）/ESDF 双 margin 惩罚/后处理与阶段二门控精化四组子配置；继承通用 `Config` 基类（与 `AlmConfig` 模式一致，供场景层统一管理配置）；状态幅值边界（v_max/a_max/δ_max/ω_max）以 reference 子配置为唯一权威来源、η_max 以 inner.steer_accel_max 为唯一权威来源，构造与 JSON 加载时经 `synchronizeAmplitudeBounds()` 同步进 cost/post_stage 的同源字段（Config 双源缺口历史教训的前置防御）；JSON 加载由配套自由函数 `LoadDdpConfigOverrides` 承载（仅覆盖显式出现的字段，未出现的保持默认值）；运动学参数（轴距）由 `PostProcessor` 直接取 `VehicleParams`，不在此重复配置 | [src/core/post_processor.h](../src/core/post_processor.h) | 已冻结 |
| `DdpReferenceBuilder` | 消费前端 `Path`，产出 DDP 阶段一求解所需的全部前端数据：等弧长重采样参考位姿（~0.05 m）、cusp/maneuver 元数据、差分初值（盒约束裁剪）、打靶节点集 $\mathcal{I}$；构造函数校验配置与车辆参数，退化输入抛 `std::invalid_argument` | [src/core/DDP/ddp_reference_builder.h](../src/core/DDP/ddp_reference_builder.h) | 已冻结 |
| `DdpReferenceBuilderConfig` | `DdpReferenceBuilder` 的配置结构体：重采样间距/固定 dt/打靶间隔/盒约束边界，默认值见头文件 | [src/core/DDP/ddp_reference_builder.h](../src/core/DDP/ddp_reference_builder.h) | 已冻结 |
| `DdpReference` / `DdpReferenceManeuver` | `DdpReferenceBuilder::build()` 的输出结构体：实际 ds/dt、$N+1$ 参考位姿、maneuver 元数据（含网格起止索引）、cusp 索引集、打靶节点集、$N+1$ 状态初值与 $N$ 控制初值（恒零）；`DdpReferenceManeuver` 为单 maneuver 元数据（sign/delta_s/delta_theta/begin_index/end_index） | [src/core/DDP/ddp_reference_builder.h](../src/core/DDP/ddp_reference_builder.h) | 已冻结 |
| `DDP_STATE_DIM` / `DDP_CONTROL_DIM` / `DDP_IDX_*` / `DdpState` / `DdpControl` / `DdpAlignedVec<T>` / `WrapAngle()` | DDP 模块共享基础件：七维状态链 $[x,y,\theta,v,a,\delta,\omega]$ 与二维控制 $[j,\eta]$ 的维度常量/分量索引/类型别名；Eigen 对齐分配器容器模板；统一角度差分 wrap（`std::remainder`），供初值提取、跟踪代价与终端约束跨 Milestone 复用 | [src/core/DDP/ddp_reference_builder.h](../src/core/DDP/ddp_reference_builder.h) | 已冻结 |
| `BicycleDynamics` | 七维自行车离散动力学核心：`step()` 半隐式 Euler 链式更新（中点朝向角 $\theta_{mid}$ 位移，二阶旋转精度、无偏）；`jacobians()` 解析雅可比 $A_k(7\times7)/B_k(7\times2)$ 全部非零元按 DDP.md 2.5 闭式；`hessians()` 完整二阶张量 $f_{xx}/f_{uu}/f_{ux}$（编译期开关 `DDP_ENABLE_FULL_HESSIAN` 控制，默认关闭）；全部栈上定长 Eigen、零堆分配、手写解析式禁 autodiff | [src/core/DDP/bicycle_dynamics.h](../src/core/DDP/bicycle_dynamics.h) | 已冻结 |
| `DdpCostEvaluator` | 阶段一代价与约束统一求值层：平滑主项/退火跟踪（角度 wrap 与初值提取/终端约束同一实现）+ 可选换挡代理 σ_β 门 + 状态幅值 AL 不等式（v²/a²/ω² 平方形态 + δ 双侧线性，门控 λ/μ·(∂g)²）+ ESDF 双 margin 惩罚 + 终点 AL 等式；输出逐阶段代价分解与 GN 导数（ℓ_x/ℓ_u/ℓ_xx/ℓ_uu/ℓ_ux）；平滑/跟踪项含 dt 因子，ESDF/AL 项为点态量不乘 dt | [src/core/DDP/ddp_cost.h](../src/core/DDP/ddp_cost.h) | 已冻结 |
| `DdpEsdfConstraint` | ESDF 双 margin 空心外点罚：复用 `ESDFMap`/`VehicleFootprintModel` 外圆集，Φ(C)=W·max(0,C)³（C² 连续），discretize-then-differentiate，时间轴 stride 抽样，梯度含圆心随 θ 旋转的几何链式法则，GN Hessian 取 Σ 6W·C·∇C·∇Cᵀ；圆心越界/ESDF 查询异常按保守侵入处理（C 取最大值、梯度为零） | [src/core/DDP/esdf_constraint.h](../src/core/DDP/esdf_constraint.h) | 已冻结 |
| `DdpStateJacobian` / `DdpControlJacobian` / `DdpStateHessianTensor` / `DdpControlHessianTensor` / `DdpMixedHessianTensor` / `DDP_IDX_JERK` / `DDP_IDX_ETA` | `BicycleDynamics` 的配套类型：状态/控制雅可比定长矩阵别名（栈上分配）；三张量类型（`std::array` 逐输出行存储）；控制分量索引（j=纵向跃度、η=前轮转角加加速度） | [src/core/DDP/bicycle_dynamics.h](../src/core/DDP/bicycle_dynamics.h) | 已冻结 |
| `BoxQpSolver` | 投影牛顿盒约束 QP 求解器（模板类 `BoxQpSolver<TDim>`，默认 `TDim = DDP_CONTROL_DIM`），头文件全实现：钳制集带梯度符号条件判定、自由子空间牛顿步、逐元素投影 + Armijo 回溯（γ=0.1）、活动集热启动；返回自由维 Hessian Cholesky 分解供增益计算，钳制行恒为零（`SolveFreeExpanded` 参考实现）；全部栈上定长，热路径零堆分配；失败语义经 `Status` 状态码上报（不抛异常），供调用方增大 $\rho_{reg}$ 后重解 | [src/core/DDP/box_qp.h](../src/core/DDP/box_qp.h) | 已冻结 |
| `AlOuterLoop` / `AlOuterLoopConfig` / `AlConstraintSnapshot` / `AlTerminationCheck` | ALTRO 式 AL 外层循环与调度状态机：自适应 $\mu^0$ clip 标定（$J_s'/\max(\|c\|^2,\varepsilon_\mu)$ clip 进 $[\mu_{min},\mu_{max}]$）；Hestenes-Powell 乘子更新（等式 $\lambda\mathrel{+}=\mu c$、不等式 $\lambda\leftarrow\max(0,\lambda+\mu g)$）；终点/幅值两组独立门控 μ 增长（$\phi=10$，标定轮只标定不增长）；跟踪权重退火 $w_{ref}(r)=w_{ref,0}\gamma^r$（首/末 maneuver 豁免掩码）；联合终止判据（终点双指标 0.05 m/1.5° + 不等式违反度 + $\|d\|_\infty$）与分项诊断输出 | [src/core/DDP/al_outer_loop.h](../src/core/DDP/al_outer_loop.h) | 已冻结 |
| `ApaDdpSolver` / `ApaDdpSolverConfig` / `ApaDdpReport` / `ApaDdpOuterRecord` / `ApaDdpStageOneResult` | APA-DDP 阶段一求解编排入口：把前端参考、MS-iLQR 内层与 AL 外层调度串成完整全局软化求解（无符号/接缝门控），外层轮次间以收敛轨迹 δ 投影热启动；内层迭代超限不致命（沿用当前迭代点），ρ_reg 溢出先冷重启实例重试一次、仍失败才判致命失败（anytime 输出最后可用轨迹）；输出结构化逐轮诊断报告（权重/μ/双指标/违反度/缺陷/内层状态/分项达标标志） | [src/core/DDP/apa_ddp_solver.h](../src/core/DDP/apa_ddp_solver.h) | 已冻结 |
| `ApaDdpStageTwoResult` / `DdpGatingPlan` / `DdpCostMultiplierState`（门控扩展） | 阶段二门控重解与代价层增量扩展（全部默认关闭、阶段一路径行为不变）：`solveStageTwo()` 支持对偶热启动（终端 λ/μ 续接 + μ⁰ 标定下限抬升）+ 三类门控 AL 约束（段内符号门 `-s_m v_k≤0`/接缝零速等式/驻留帽 `|v|−cap`）+ 独立门控 μ 调度；`DdpGatingPlan` 逐节点编码符号门/接缝查表/驻留帽；代价层「阶段一禁止启用」断言防御（计划与乘子仅一方在场 → `std::logic_error`） | [src/core/DDP/apa_ddp_solver.h](../src/core/DDP/apa_ddp_solver.h) / [src/core/DDP/ddp_cost.h](../src/core/DDP/ddp_cost.h) | 已冻结 |
| `DdpPostStage` / `DdpPostStageConfig` / `DdpSignRun` / `DdpSeamPlan` / `DdpSeamReport` / `DdpPostStageDiagnostics` / `DdpPostStageResult` / `DdpDwellEdit` | 后处理六步编排：带滞回符号游程分析（$\varepsilon_v$=0.02 m/s、Reeds-Shepp 档位恢复）；拓扑修剪红线封装（复用 `topology_cleaner`、首末保护/PIVOT 失败语义）；$T_{resteer}$ 双积分 bang-bang 三角/梯形两分支 + 逐接缝驻留窗定宽（$m_j=\lceil\max(T_{resteer},T_{shift})/(2dt)\rceil$）；阶段一解→阶段二网格热启动映射；驻留插入线性时间拉伸（只拉伸时间轴、不改空间路径）；六项校验清单（碰撞/终点/运动学/幅值/控制过冲/接缝完整性）；五类回退出口 + 结构化诊断 | [src/core/DDP/ddp_post_stage.h](../src/core/DDP/ddp_post_stage.h) | 已冻结 |

> 以下模块目录当前为空文件（规划中，尚未实现），暂不登记接口：`src/spatial/sfc_corridor.h/.cpp`、`src/core/obb_inflator.h/.cpp`、`src/scene/planning_scene.h/.cpp`。待其实现时再补充登记。

## StcSQP 框架内部接口（联合治理）

> `third_party/StcSQP` 本身按第三方依赖对待、不计入本仓库业务代码规范（见 [docs/architecture.md](architecture.md)
> 第 4 节），下表只登记本仓库侧对 `third_party/StcSQP` 主动修改的那几个内部接口，作为本仓库侧的变更留痕；
> 这些接口的权威定义与设计意图仍以 `third_party/StcSQP/design_document.md`/`AGENTS.md` 为准，
> 两边文档必须同步更新，不能只登记在一处。

| 接口 | 设计意图（一句话） | 头文件链接 | 状态 |
|---|---|---|---|
| `stc_SQP::CostTerm` | 代价项求值纯虚接口；新增组合求值方法 `evaluateGradientAndHessian()`（默认转发到既有 `evaluate()`+`gradient()`+`hessian()`，向后兼容）与 `clone()`（供 `assembleQP()` 并行化使用）；`CircleFootprintEsdfPenaltyCost`/`CompositeCost`/`QuadraticTrackingCost` 均已实现对应方法 | [third_party/StcSQP/src/costs/cost_term.hpp](../third_party/StcSQP/src/costs/cost_term.hpp) | 已完成 |
| `stc_SQP::Constraint` | 约束求值纯虚接口；已评估 `std::variant` 去虚拟化方案，benchmark 无收益，决定不合入，保留虚函数机制 | [third_party/StcSQP/src/constraints/constraint.hpp](../third_party/StcSQP/src/constraints/constraint.hpp) | 已评估不采纳 |
| `stc_SQP::HPIPMQPSolver` | HPIPM 求解器包装；已将 `setWarmStart()` 从空实现改为真正的 IPM 跨迭代热启动，新增 `lastIterations()`/`totalIterations()` 读取 IPM 迭代统计 | [third_party/StcSQP/src/qp/hpipm_solver.h](../third_party/StcSQP/src/qp/hpipm_solver.h) | 已完成 |
| `stc_SQP::SQPSolver` | Full SQP 主循环；已为 `assembleQP()`/`assembleCost()` 补充 OMP 并行路径，并在 `solveQP()` 中接入跨迭代热启动调用与 `use_qp_warm_start` 配置开关 | [third_party/StcSQP/src/sqp/sqp_algorithm.h](../third_party/StcSQP/src/sqp/sqp_algorithm.h) | 已完成 |
| `stc_SQP::StageSegment` | 单段 OCP 描述；已实现 `std::variant` 改造并 benchmark，无收益，已评估不采纳，保留原有 `shared_ptr<Constraint/CostTerm>` 字段类型 | [third_party/StcSQP/src/ocp/multi_stage_ocp.h](../third_party/StcSQP/src/ocp/multi_stage_ocp.h) | 已评估不采纳 |

## proto 兼容性说明

`proto/apa_post_process.proto` 中 `Path`/`Maneuver` message 携带的路径点始终只使用
`apa::post_processor::Pose`（`x`/`y`/`theta`），**不新增字段承载 `PathPoint` 的派生量**
（`kappa`/`v`/`delta`/`a`/`delta_dot`）。这些派生量目前只在进程内产生和消费，不需要
持久化或跨进程传递；C++ 侧的 `Path::FromProto`/`toProto` 负责在 `Pose`（proto 反序列化产物）
与 `PathPoint`（`Path`/`Maneuver` 的内部存储类型）之间转换。若未来确有需要持久化某个派生量，
应先按 [.agents/instructions/dependency-policy.md](../.agents/instructions/dependency-policy.md)
之外的正常 proto 变更流程评估，再更新本节。

## 变更记录

| 日期 | 接口 | 变更内容 | 原因 |
|---|---|---|---|
| 2026-07-06 | `Pose` | 移除 `kappa` 字段，恢复为纯 `{x, y, theta}` | 曲率是路径规划语境下的派生量，不应耦合进通用位姿类型 |
| 2026-07-06 | `PathPoint` | 从空壳继承类补充为真正独立的路径点类型：新增 `kappa`/`v`/`delta`/`a`/`delta_dot` 五个派生量，默认值均为 `std::numeric_limits<double>::quiet_NaN()`，通过 `hasXxx()`/`getXxx()`/`setXxx()` 三件套访问（`getXxx()` 未设置时抛 `std::logic_error`），改用 `class`（而非 `struct`）+ `protected` 字段体现"不一定存在"的封装语义 | 承接原本错误挂在 `Pose` 上的曲率职责；补充 NMPC 优化轨迹的状态/控制量（`v`/`delta`/`a`/`delta_dot`），使 `NmpcSolver` 输出不再丢弃这些信息；用 has/get 强制调用方在使用前确认可用性，避免误用哨兵值 |
| 2026-07-06 | `Maneuver` | 模板构造函数收窄为只接受 `PathPoint`/`std::vector<PathPoint>`，不再接受裸 `Pose`/`std::vector<Pose>` | 避免调用方误以为可以自行提供有意义的 `kappa`，让类型系统在编译期堵住误用 |
| 2026-07-06 | `Path` | `front()`/`back()`/`forEach` 回调对外暴露类型从 `Pose` 改为 `PathPoint`；内部曲率估计静态方法操作 `std::vector<PathPoint>&` | 与 `Maneuver` 存储类型变更保持一致 |
| 2026-07-06 | `Path` | 曲率计算时机从 `addPoint()` 增量刷新改为 `finalize()` 统一批量计算；移除 `refreshTailCurvature()` | 避免"草稿曲率/最终曲率"两套状态并存的复杂度，简化曲率估计算法本身 |
| 2026-07-06 | `Pose` | 移除 `kappa` 字段后正式冻结为纯 `{x, y, theta}` | 曲率职责已迁移到 `PathPoint`，通用位姿类型不再携带路径规划派生量 |
| 2026-07-06 | `PathPoint` | 在 `Path`/`Maneuver`/`NMPC`/`Visualizer` 中全面接入 | `Path`/`Maneuver` 存储类型从 `Pose` 切换为 `PathPoint`，NMPC 输出回填 v/delta/a/delta_dot |
| 2026-07-06 | `Maneuver` | 模板构造函数收窄为只接受 `PathPoint`/`std::vector<PathPoint>` 后冻结 | 避免调用方绕过 `Path` 曲率估计自行提供 `kappa` |
| 2026-07-06 | `PathToOcpConverter` | `buildCumulativeArcLength`/`interpolateAtArcLength` 改用 `PathPoint` 后冻结 | 与 `Maneuver` 存储类型保持一致 |
| 2026-07-06 | `NmpcSolver` | `ToPath`/`pruneShortestSegment` 改用 `PathPoint` 并回填 v/delta/a/delta_dot 后冻结 | 优化结果的状态/控制量不再丢失 |
| 2026-07-06 | `Path` | `addPoint()` 不再刷新曲率，移除 `refreshTailCurvature()`；`finalize()` 统一批量计算所有 `Maneuver` 的曲率 | 避免"草稿曲率/最终曲率"两套状态并存，简化曲率估计算法 |
| 2026-07-06 | `Path` | Pose/PathPoint 重构收尾审计通过，将 `Path` 接口状态更新为"已冻结" | 此前几轮重构引入的变更已落地并验证，接口进入稳定状态 |
| 2026-07-06 | `BSplineSmoother` / `BSplineSmootherConfig` | 新增四次 B 样条分段平滑器接口，登记构造函数与 `smooth()` 主入口；配置字段默认值来源在对应设计文档中记录 | 预处理管线首个落地接口，为后续速度规划、微分平坦、自适应重采样等阶段提供统一曲线/弧长表/密集配点输入 |
| 2026-07-06 | `VehicleParams` | 新增 `max_accel`/`max_decel`/`max_steer_rate` 字段，proto 同步为 optional 字段；`FromProto` 对缺失字段使用默认安全值 | 纵向速度规划需要加减速 box bound，`max_steer_rate` 提前落地供换挡原地打轮补丁复用 |
| 2026-07-06 | `SpeedProfilePlanner` / `SpeedProfilePlannerConfig` / `SpeedProfileInput` / `SpeedProfileResult` | 新增空间域 $v^2$ 凸优化速度规划接口；配置、输入、输出结构体一并登记 | 为预处理管线提供纵向速度规划能力，衔接 BSplineSmoother 输出的密集配点 |
| 2026-07-06 | `DifferentialFlatnessSolver` / `DifferentialFlatnessSolverConfig` / `DifferentialFlatnessInput` / `DifferentialFlatnessResult` | 新增微分平坦解析补全接口；配置、输入、输出结构体一并登记 | 为预处理管线提供状态/控制量解析补全能力，衔接 BSplineSmoother 几何导数与 SpeedProfilePlanner 速度剖面 |
| 2026-07-06 | `AdaptiveResampler` / `AdaptiveResamplerConfig` / `AdaptiveResamplerSegmentInput` / `AdaptiveResamplerResult` | 新增轨迹自适应重采样与维数固化接口；配置、输入、输出结构体一并登记 | 为预处理管线提供固定维数打靶点序列生成能力，衔接 BSplineSmoother 密集配点、SpeedProfilePlanner 速度剖面与 DifferentialFlatnessSolver 状态/控制量 |
| 2026-07-07 | `StaticCorridorBuilder` / `StaticCorridorBuilderConfig` | 新增静态安全走廊构建器接口，登记 `build()` 主入口与 `hard_margin`/`soft_margin` 配置字段 | 为预处理管线提供一次性 ESDF 线性化走廊生成能力，替代 NMPC 热循环中动态 ESDF 查询 |
| 2026-07-07 | `BSplineSmoother` | 将 `buildKnotVector()` 与 `computeBasisAtU()` 从 protected 提升为 public | 预处理管线需要在构造 SpeedProfileInput / DifferentialFlatnessInput 时从平滑结果中提取 B 样条几何导数；这两个方法是纯工具函数，不涉及内部状态变更，提升访问级别不会破坏封装 |
| 2026-07-07 | `PreprocessingPipelineConfig` / `PreprocessingPipeline` / `PreprocessingPipelineResult` | 新增预处理管线组装器接口，登记 `run()` 主入口、配置聚合结构体与结果结构体 | 为 NMPC 提供端到端的 Warm Start 生成能力，把此前五个独立预处理阶段串成一条可复用的管线 |
| 2026-07-07 | `NmpcSolverConfig` | 新增 `static_corridor_C`（`std::optional<Eigen::MatrixXd>`）与 `static_corridor_d`（`std::optional<Eigen::VectorXd>`）字段，标记 `esdf_penalty_weight` 为过渡期废弃字段 | 静态线性走廊（NMPC.md 3.5 节）与现有动态 ESDF 软代价是替代关系；过渡期保留 `esdf_penalty_weight` 作为兜底开关，当 `static_corridor_C` 未提供时仍使用动态 ESDF 软代价维持向后兼容。完整的 HPIPM 静态走廊约束注入留待后续战术改动 |
| 2026-07-07 | `StaticCorridorBuilderConfig` | `hard_margin` 默认值从 0.08（8cm）改为 0.05（5cm），增加交叉引用注释指向 `BSplineSmootherConfig::collision_margin` | 碰撞安全裕度统一：`hard_margin` 与 `collision_margin` 代表同一物理量 $d_{margin}$，此前各自独立配置默认值（5cm vs 8cm）存在跨阶段数值不自洽风险 |
| 2026-07-07 | `BSplineSmootherConfig` | `collision_margin` 注释补充交叉引用，指向 `StaticCorridorBuilderConfig::hard_margin` 与 `PreprocessingPipelineConfig::collision_safety_margin` | 字段默认值本身未变（已是 5cm），仅补充注释防止未来有人单方面修改其中一个而不同步另一个 |
| 2026-07-07 | `PreprocessingPipelineConfig` | 新增 `collision_safety_margin` 字段（默认 0.05），作为跨阶段统一安全裕度的唯一权威来源；`PreprocessingPipeline` 构造函数自动将其传播到 `bspline.collision_margin` 与 `corridor.hard_margin` | 消除此前 `BSplineSmoother` 与 `StaticCorridorBuilder` 各自独立配置安全裕度（5cm vs 8cm）的不自洽风险，提供单一配置入口 |
| 2026-07-07 | `NmpcSolverConfig` | 新增 `max_theta_deviation_from_ref` 字段（默认 0.0，即关闭信赖域约束）；启用时建议初始值 0.06 rad，按真实数据集调参确定最终取值 | 静态走廊一阶泰勒线性化只在状态偏离 $Z_{ref}$ 较小时成立，此前设计未显式约束该偏差范围 |
| 2026-07-07 | `NmpcSolver` | `optimize()` 内部新增信赖域约束（`ThetaTrustRegionConstraint`）与静态走廊约束（`StaticCorridorLinearConstraint`）的注入逻辑；静态走廊与动态 ESDF 软代价为替代关系，当 `static_corridor_C/d` 提供且数据一致时使用静态走廊，否则兜底使用 `esdf_penalty_weight` 动态查询机制 | HPIPM 层静态走廊约束注入 + 信赖域 box 约束，两个前置条件已同时落地 |
| 2026-07-07 | `PathToOcpConverter` | `buildSegment()` 新增 `stage_params` 填充逻辑（p(0)=局部步索引，p(1)=参考航向 theta_ref），供下游约束类按步读取参数 | 为信赖域约束与静态走廊约束提供 per-step 上下文（theta_ref / 步索引） |
| 2026-07-07 | `PreprocessingPipelineConfig` / `PreprocessingPipelineResult` / `PreprocessingPipeline` | 新增调试数据透传：`PreprocessingPipelineConfig::enable_debug_output`（默认 `false`）、`PreprocessingPipelineResult::debug_maneuver_outputs`、`PipelineDebugManeuverOutput`；`PreprocessingPipeline::run()` 开启开关时移动中间产物到结果 | 为 `Visualizer::plotPipelineDiagnostics` 提供各阶段中间产物，默认关闭不影响生产路径内存占用 |
| 2026-07-07 | `Visualizer` / `DetailSeriesData` | 扩展 `DetailSeriesData` 动力学/控制/时间/走廊松弛量字段；新增 `Visualizer::plotPipelineDiagnostics()` 2x2 诊断入口与 `drawDetailSubplotDualY()` 双 Y 轴子图模板方法 | 为预处理管线全断面离线排障提供统一可视化视图 |
| 2026-07-07 | `PreprocessingPipelineResult` / `VehicleFootprintModel` | 复审修复：`PreprocessingPipelineResult` 新增 `original_z_ref`、`hard_margin_used`、`soft_margin_used`、`outer_row_num_used`；`VehicleFootprintModel` 新增 `getOuterRowNum()` | 使 Visualizer 侧的原始路径绘制、slack_true / 隔离墙计算与 PreprocessingPipeline 实际使用的参数保持一致 |
| 2026-07-08 | `Visualizer` | 预处理管线诊断视图布局从 2x3 重构为 2x2：第一列完整显示空间几何；第二列按 s-domain / index-domain 分上下两行，每行内多条曲线垂直堆叠且各自拥有独立 Y 轴；移除原 Subplot 6 静态走廊 slack 对比图 | 避免不同量级信号共享 Y 轴导致的互相压缩，提升 spatial 图的空间利用率 |
| 2026-07-10 | `PreprocessingPipelineConfig` / `BSplineSmootherConfig` / `StaticCorridorBuilderConfig` / `NmpcSolverConfig` / `AdaptiveResamplerConfig` | 碰撞检测重大更新：移除物理安全裕度（`collision_safety_margin`/`collision_margin`/`hard_margin`/`esdf_safety_margin`/`obstacle_density_margin` 默认值从 5cm/5cm/5cm/20cm/5cm 统一改为 **0**）；`collision_validation_tolerance` 从 2cm 改为 `1e-4`（0.1mm，适配三次方碰撞惩罚的梯度消失）；新增 `kCollisionEpsilon = 1e-6` 常量用于浮点安全比较。`soft_margin`（18cm）独立保留用于降低乘员压迫感 | 外圆（Outer Circles）本身已超出车辆矩形轮廓边界，无需额外叠加物理安全裕度；`soft_margin` 继续提供舒适缓冲。详见 [docs/NMPC.md](NMPC.md) 3.1 节"碰撞检测数值容差的设计原则" |
| 2026-07-10 | `Visualizer` | `plotPipelineDiagnostics()` 新增可选 `VehicleFootprintModel*` 参数；index-domain 子图在传入 footprint_model 时绘制 `Z_ref` 每点处所有外圆的最小安全余量 `min(d_esdf - r)`，并叠加 `0m` 与 `soft_margin` 参考线，未传入 footprint_model 时回退到绘制 `delta_t` | 满足离线排障时对每个预处理采样点安全余量的直接观测需求；保持向后兼容，旧调用方无需改动 |
| 2026-07-08 | `ESDFMap` | 内部实现优化（SoA→AoS 数据布局、双线性插值索引预计算、`std::floor`→`static_cast<int>`、内部计算 float 化），**公开接口签名不变**（`getDistAndGrad`/`getDist` 返回类型保持 `std::pair<double, Eigen::Vector2d>`/`double`） | 查询热路径性能优化，不影响冻结接口契约，下游调用方无需任何改动 |
| 2026-07-09 | `BSplineSmootherConfig` | `lbfgs_max_iterations` 默认值从 100 收紧到 80，`lbfgs_max_linesearch` 默认值从 100 收紧到 20 | 按调参-验证-回滚流程实测：100/100 基线 → OMP 并行化 → 单变量单调收紧；`max_iterations` 在 50 时触发测试失败，在 60 时于四数据集之一的 data7 出现 199.44um 侵入深度回退，最终确定为 80/20；`max_linesearch` 在 20 时仍满足 Safety Gate |
| 2026-07-09 | `stc_SQP::CostTerm` | 新增 `evaluateGradientAndHessian()` 组合求值接口（默认向后兼容转发）与 `clone()` 纯虚方法；`CircleFootprintEsdfPenaltyCost`/`CompositeCost` 覆写组合求值以消除重复 ESDF 查询，`QuadraticTrackingCost` 实现 `clone()`；`SQPSolver::assembleCost()` 优先调用组合求值接口 | 为 `assembleQP()` OpenMP 并行化提供与 `Constraint::clone()` 对称的 cost 克隆能力，并消除 `CircleFootprintEsdfPenaltyCost` 每步每次 SQP 迭代 2~3 次的 ESDF 重复查询 |
| 2026-07-09 | `stc_SQP::SQPSolver` | 新增 `thread_cost_clones_` 与 per-thread `CostScratch`，`assembleQP()`/`assembleCost()` 补齐与 `linearize()` 对称的 OpenMP 并行路径（沿用 `options_.omp_parallel_threshold` 阈值） | 将 `linearize()` 已有的 OMP 并行基础设施复用到 cost 装配阶段 |
| 2026-07-09 | `stc_SQP::HPIPMQPSolver` | `setWarmStart()` 从空实现改为真正的 HPIPM IPM primal 热启动；`solve()` 内部自动维护上一次成功解的缓存，维度不匹配或求解失败时退化/清空为冷启动；新增 `lastIterations()`/`totalIterations()` 用于读取 IPM 迭代统计 | 在 Full SQP 循环中跨迭代复用上一次 QP 解作为 IPM 初值，降低 IPM 迭代次数 |
| 2026-07-09 | `stc_SQP::SQPSolver` | `SQPSolverOptions` 新增 `use_qp_warm_start` 开关（默认 `false`）；`solveQP()` 在 Full SQP 非首次迭代且开关开启时调用 `qp_solver_->setWarmStart(qp_solution_)` | 将跨迭代热启动接入 Full SQP 主循环；RTI 单步模式不受影响 |
| 2026-07-09 | `PathToOcpConverter` / `SegmentProfile` | 拆分 `PathToOcpConverter` 职责。新增 `SegmentProfile` 结构体描述单段离散化；新增 `computeSegmentProfiles()` / `generateInitialGuess()` / `buildOcp()` 三个公开入口；`convert()` 内部调用三者并保持原有行为；`buildSegment()` 签名改为接受 `SegmentProfile` + `terminal_x_ref` + `theta_refs`，与初始猜测来源解耦 | 为后续接入预处理管线产出的 Z_ref/U_ref/非均匀 delta_t 提供清晰扩展点，避免复制粘贴约束注入逻辑 |
| 2026-07-09 | `NmpcSolver` | 新增 `optimize(const MultiStageOCP&, const Trajectory&, const ESDFMap&)` 重载；原 `optimize(Path, ESDFMap)` 内部调用 `PathToOcpConverter::convert()` 后转发到该重载；约束注入（信赖域/静态走廊/动态 ESDF 软代价）与求解逻辑下沉到 `solveOcp()` 共享 | 提供不修改公开 `Path` 入口即可注入预装配 OCP/初始猜测的最小侵入式扩展点 |
| 2026-07-09 | `PreprocessingToOcpConverter` | 新增预处理管线输出 → OCP 转换器，直接消费 `PreprocessingPipelineResult` 的 `z_ref`/`delta_t`，把非均匀步长通过 `StageSegment::dt_array` 接入；同时按 OCP 总步数截断 `c_matrix`/`d_vector`，与 `NmpcSolver` 的 `expected_corridor_rows` 对齐 | 把预处理管线产出真正当作 Warm Start 喂给 `NmpcSolver`，替代 `PathToOcpConverter` 的简化三次多项式猜测 |
| 2026-07-09 | `src/main.cpp` | 生产入口切换为 `Path → PreprocessingPipeline → PreprocessingToOcpConverter → NmpcSolver` 完整链路；新增"NMPC 失败但预处理成功时回退到预处理轨迹"兜底，日志/`OptimizeResponse.message` 明确区分四态 | 完成预处理管线与 NMPC 的端到端接线，满足用户需求"如果优化失败但是预处理成功可以考虑用预处理的路径" |
| 2026-07-09 | `AdaptiveResampler::assembleFinalTrajectory` | 修复相邻常规段空间连续但无换挡补丁时，最终 `delta_t.size()` 比 `points.size() - 1` 少 `segment_count - 1` 的问题；当相邻段终点/起点距离小于 1e-3m 时自动补一条连接 `delta_t` | 保证真实泊车数据（段间共享换挡点）下 `PreprocessingPipelineResult.delta_t` 长度与 `z_ref` 严格匹配，使下游 `PreprocessingToOcpConverter` 可直接消费非均匀 `dt_array` |
| 2026-07-09 | `PreprocessingToOcpConverter` | 按 `z_ref` 速度符号将单段 OCP 拆分为多段 `MultiStageOCP`；每段独立生成 `dt_array`、stage_params、终端跟踪代价与单向速度箱约束；静态走廊系数按总 OCP 步数截断 | 解决多 maneuver 真实数据上 NMPC 首迭代 QP 不可行问题，允许 Warm Start 在保持方向一致的各段内满足箱约束 |
| 2026-07-09 | `PostProcessor` / `AdaptiveRetryConfig` | 新增 `PostProcessor` 完整链路封装与 `AdaptiveRetryConfig`；默认参数 NMPC 完全失败时，临时拉大 `AdaptiveResamplerConfig::nominal_step_s`（默认 ×2、×3）重试，并在最后一次重试关闭静态走廊作为兜底；所有重试只修改局部配置副本 | 满足用户需求"预处理间隔拉长重试、用完必须恢复默认值"，并保证调用方默认配置不被污染；解决 data6.json 静态走廊与 Warm Start 首迭代不可行问题 |
| 2026-07-15 | `NmpcSolverConfig::esdf_penalty_weight` | 语义从"与 `static_corridor_C/d` 互斥的替代机制"改为"与静态走廊协同的梯度引导机制"——`NmpcSolver::solveOcp()` 内部触发条件从 `!use_static_corridor && esdf_penalty_weight > 0` 放宽为 `esdf_penalty_weight > 0`（不再要求走廊未启用）。默认值仍为 0（关闭），需按数据集显式调参开启 | 用户明确需求：静态走廊硬约束保证"每次 SQP 迭代无论是否收敛都安全（不穿模）"，ESDF 直接代价提供真实非线性梯度以加速/改善收敛，两者互不冲突（代价只影响目标函数，不影响可行域），不应互斥 |
| 2026-07-19 | `src/core/ALM/`（新模块，规划中） | 新增"待实现：ALM 模块核心接口"一节，登记 `BlockTridiagonalSolver`/`MincoTrajectory`/`BicycleKinematicsExtractor`/`AlmManeuverSegmenter`/`AlmEsdfPenalty`/`AlmPreprocessor`/`AlmSolver` 等规划中接口的目标头文件路径与设计意图，对应 [docs/ALM.md](ALM.md) 与 [docs/milestones.md](milestones.md) 登记的 ALM 模块开发计划 | 与 [docs/architecture.md](architecture.md) 3.7 节同步：ALM 是与 NMPC 并列的第二条后处理算法路径，尚未创建任何源文件，先落地接口规划供 Milestone 拆分与后续 Dev Agent 实现参考 |
| 2026-07-15 | `NmpcSolver::solveOcp` | `ng_max` 计算从手工按约束类型硬编码行数累加改为直接调用 `stc_SQP::strategy_internal::computeOcpNgMax(mutable_ocp)`，对实际已装配的各段约束求和取最大值 | 手工累加公式曾遗漏"某类终端约束实际是否被注入"的条件判断（`terminal_position_error_threshold`/`terminal_heading_error_threshold_deg` <= 0 时 `TerminalPoseBoxConstraint` 不再注入，但原公式仍无条件加 6 行），导致 `qp_data.ng_max` 与 HPIPM 构造时的 `ng` 不一致，触发 `QPSolverStatus::INVALID_ARGUMENT`（诊断 data6.json 时复现）；改为动态求和后消除整类潜在的维度不一致 bug |
| 2026-07-15 | `StaticCorridorBuilder::computeDScalar` | 内部实现新增"自洽性修正"：当参考点 `Z_ref` 自身已违反安全边界（`dist_ref < radius + margin`，多发生于 `BSplineSmoother` 碰撞预推未能完全消除侵入的极端困难场景）时，为该约束行补偿等量违反深度 `violation = max(0, radius + margin - dist_ref)`，使约束在 `Z = Z_ref` 处恰好取等号而非直接不可行；`dist_ref >= radius + margin` 时 `violation = 0`，与原公式完全一致，不影响既有已验证场景 | 原公式在参考点已侵入时会产出连 Warm Start 自身都无法满足的硬约束，导致 HPIPM 在第 0 次 SQP 迭代即失败；修正后保证"线性化安全裕度不允许比参考点当前状态更差"，即使无法达到理想安全裕度也不会让 HPIPM 因起点不可行而直接拒绝求解 |
| 2026-07-15 | `PostProcessor::runSingleAttempt` | 内部实现修复：NMPC 求解失败或碰撞超标回退到预处理轨迹的两条分支，此前直接返回未经拓扑清洗的 `Path`，现统一改为调用与 NMPC 成功路径共享的 `applyTopologyCleanup` 辅助 lambda | 修复回归 bug：预处理轨迹中的换挡/起始转向零速补丁段会被 `Path::finalize()` 按方向变化拆分成比原始路径更多的 maneuver（实测 data1.json 曾回退产出 12 段，超过初始 10 段），直接违背"机动段数不劣化"的验收要求 |
| 2026-07-15 | `NmpcSolverConfig::max_iter` | 默认值从 1000 收紧回 300 | 已在四数据集上验证 300 次迭代内可获得可用结果；过大上限（1000/2000）会把单次求解耗时推高到分钟级且未观察到额外解质量收益，拖慢调参迭代反馈速度 |
| 2026-07-15 | `NmpcSolver::solveOcp` / `NmpcSolverConfig` | `ThetaTrustRegionConstraint`/`PositionTrustRegionConstraint` 的注入条件从"必须启用静态走廊"改为"仅取决于配置值本身 > 0"，任意走廊模式（静态/迭代 ESDF）下均生效；`max_position_deviation_from_ref` 默认值从 0.0（关闭）改为 0.15（历史调参验证值） | 修复"NMPC 优化后轨迹在内部机动段呈折线/直线"的视觉缺陷：内部机动段代价矩阵仅跟踪 v/delta、不跟踪 x/y/theta，唯一能把中间打靶点约束在参考曲线附近的机制此前被硬编码为仅静态走廊模式下生效，导致回退到迭代走廊时完全不受约束 |
| 2026-07-15 | `PathToOcpConverter` | `buildSegment()` 签名新增 `x_refs`/`y_refs` 参数（`protected` 内部方法，非公开冻结接口），与已有 `theta_refs` 并列传入，用于填充 `stage_params.p(3)/p(4)` | 此前该入口从未填充 p(3)/p(4)，若调用方启用 `PositionTrustRegionConstraint` 会错误地把轨迹约束到坐标原点附近；修复后与 `PreprocessingToOcpConverter` 行为一致 |
| 2026-07-15 | `NmpcSolver::solveOcp` / `NmpcSolverConfig` | 移除 `TerminalPoseBoxConstraint`/`TerminalFinalStateConstraint` 的注入逻辑，`use_terminal_final_state_constraint` 字段随之删除；终端精度改为完全依赖终端跟踪代价（`terminal_position_weight`/`terminal_heading_weight = 1e5`）。`terminal_position_error_threshold`/`terminal_heading_error_threshold_deg` 字段保留，语义收窄为"仅质量门阈值"，不再驱动任何硬约束注入 | 用户明确要求"放开一切束缚"重新设计：诊断确认静态走廊 + 终端位姿盒 + 终端状态硬约束 + 信赖域四类硬约束同时作用于终端区域是 HPIPM `UNKNOWN_ERROR` 的主要诱因之一；移除终端硬约束后 `data1`/`data3` 不再触发该错误，求解成功率与速度显著提升，详见 `docs/NMPC.md` 6.6 节 |
| 2026-07-15 | `PostProcessor::runSingleAttempt`（剪枝内部实现） | 剪枝候选的求解从单一配置改为多组局部配置重试（沿用与 `solveFullPipeline` 相同的 `PreprocessingPipelineConfig` 参数化），任一组成功即接受 | 诊断发现剪枝失败的真正瓶颈在预处理层（B样条平滑）而非 NMPC/HPIPM，本改动作为兜底保留，主要修复见下一行 `BSplineSmoother` 变更 |
| 2026-07-15 | `BSplineSmoother`（内部实现，非公开签名变更） | L-BFGS 精修失败时的回退目标从"碰撞预推之前的 `initial_x`"改为"碰撞预推之后的检查点 `prepushed_x`"；"双倍碰撞权重重试"步骤的出发点保持 `initial_x` 不变 | 修复真实 bug：预推能把新合并几何的侵入深度从米级压到毫米级，但精修失败时错误地把这部分进度整体丢弃，导致碰撞校验永远针对未逃生的原始高侵入深度判定失败，使预处理管线在"实际已经足够安全"的场景下无谓拒绝、连累 NMPC 根本没有机会运行。详见 `docs/NMPC.md` 6.6.3/6.6.4 节与 `docs/known-limitations.md` 对应条目 |
| 2026-07-15 | `PostProcessor::runSingleAttempt` | 彻底删除机动段剪枝循环（`RemoveShortestManeuver` 调用、`prune_retry_configs`、`solvePrunedWithRetries`、四重门禁判定），简化为单次 `solveFullPipeline` 调用 | 用户明确要求放弃"预处理层盲剪+全链路重跑验证"的段数削减机制（已证明效果不佳），改由 NMPC 内生顺滑机制（见下一行）承担段数削减 |
| 2026-07-15 | `util::topology_cleaner`：`RemoveShortestManeuver` | 函数本体删除（`ClassifyAndResetManeuvers`/`ReconstructPath` 保留） | 同上；该函数仅服务于已删除的剪枝循环，无其它调用方 |
| 2026-07-15 | `PreprocessingToOcpConverter` / `NmpcSolver` | 状态维度从 5 扩展为 7：新增 `BicycleModelJerk` 动力学模型（本项目自定义，不修改 `third_party/StcSQP`），`a`、`delta_dot` 从控制量升级为状态量，新增控制量 `[jerk, ddelta_dot]`；`ThetaTrustRegionConstraint`/`PositionTrustRegionConstraint`/`StaticCorridorLinearConstraint`/`IterativeCorridorConstraint` 的 `Cx` 构造改为按 `x.size()` 动态适配（兼容 5 维/7 维两种入口） | 用户要求真正实现文档 4.2 节设计的 $J_{smooth}$（跨 stage 控制量差分代价），让 NMPC 求解本身具备"熔化冗余换挡"的顺滑压力，取代预处理层剪枝；控制量升阶（rate-as-input）是实现跨 stage 差分代价的标准工业界做法，无需交叉 Hessian 或修改第三方框架 |
| 2026-07-15 | `PathToOcpConfig`（`path_to_ocp_converter.h`） | 新增 `max_jerk=10.0`、`max_steer_angular_accel=5.0`、`smoothing_jerk_weight=1e-1`、`smoothing_steer_accel_weight=1e-1` 四个字段（仅 `PreprocessingToOcpConverter` 消费）；`accel_limit`/`steer_rate_limit` 复用为状态 box bound（语义不变，物理极限从控制量平移到状态量） | 承接上一行状态增广改动 |
| 2026-07-15 | `IterativeCorridorConstraint`/`StaticCorridorLinearConstraint`（内部实现） | 修复两处硬编码 `Cx = Matrix::Zero(ng, 5)` 的遗留 bug，改为按实际状态维度动态构造 | 状态增广后暴露的真实 bug，此前从未被端到端测试覆盖（现有测试只检查转换结果维度，未真正驱动一次完整求解），详见 `docs/known-limitations.md` 对应条目 |
| 2026-07-15 | `NmpcSolverConfig`/`PositionTrustRegionConstraint`/`NmpcSolver::solveOcp` | 位置信赖域从硬约束改为 HPIPM 原生软约束（纯 L2 二次跟踪代价）：新增 `position_tracking_weight` 字段（对应 $W_x=W_y$）；`max_position_deviation_from_ref` 语义从"硬约束宽度"改为"软代价死区宽度"，默认值从 0.15 改为 0.01；`PositionTrustRegionConstraint` 构造函数校验从"必须 > 0"放宽为"必须 >= 0" | 理论分析发现硬信赖域是非黑即白的可行性门槛，会一票否决"偏移量略超阈值但更优"的机动段压缩方案，直接阻断 $J_{smooth}$ 的段数削减效果；改为软代价后单元测试验证了理论（合成场景轨迹压缩 65.5%），但四数据集实测仍未达成段数削减且 `data7` 终点精度出现回归，详见 `docs/NMPC.md` 6.8 节 |
| 2026-07-15 | `NmpcSolverConfig`/`ThetaTrustRegionConstraint`/`NmpcSolver::solveOcp` | 航向信赖域同样从硬约束改为 HPIPM 原生软约束（与位置跟踪同一套机制）：新增 `theta_tracking_weight` 字段（对应 $W_\theta$）；`ThetaTrustRegionConstraint` 构造函数校验从"必须 > 0"放宽为"必须 >= 0" | 不再依赖静态走廊，航向信赖域"服务走廊线性化有效性"的存在理由随之消失，改为与位置跟踪一致的软代价跟踪，详见 `docs/NMPC.md` 6.9 节 |
| 2026-07-15 | `PreprocessingPipelineConfig::use_static_corridor` / `PostProcessor::AdaptiveRetryConfig::use_static_corridor_flags` | 默认值分别从 `true`/`{true, false}` 改为 `false`/`{false, false}` | 默认弃用静态走廊，NMPC 侧回退到每轮重新线性化的 `IterativeCorridorConstraint` 作为唯一碰撞安全硬约束来源（保留迭代走廊为硬约束）；四数据集实测未实现段数削减，但意外修复了 `data3` 长期存在的 HPIPM `UNKNOWN_ERROR`，详见 `docs/NMPC.md` 6.9.3 节 |
| 2026-07-15 | `NmpcSolverConfig::position_tracking_weight`/`theta_tracking_weight` | 默认值从 `1e4` 改为 `0.0`（默认完全关闭 $J_{process}$ 持续跟踪参考轨迹 $Z_{ref}$ 的机制，代码/`Constraint` 类本身保留） | 经论文对照验证：持续跟踪包含冗余换挡的粗参考轨迹本身，与"熔化冗余换挡"目标方向相反；此前阶段只软化了该机制的强制程度，从未质疑其存在的合理性，详见 `docs/NMPC.md` 6.10 节 |
| 2026-07-15 | `PathToOcpConfig`（`path_to_ocp_converter.h`） | 新增 `global_target_position_weight=1e-3`、`global_target_heading_weight=1e-3` 两个字段：对**每一个**打靶步（含终端段）施加向**常量**停车目标位姿的二次牵引代价，复用已有的 `QuadraticTrackingCost`；`PreprocessingToOcpConverter::buildSegment`/`PathToOcpConverter::buildSegment`（后者新增 `global_target_x_ref` 参数）均已接入 | 参考 Zhang et al.《Automatic parking trajectory planning in narrow spaces based on Hybrid A* and NMPC》(Sci Rep 2025) Eq.(10) 的 $J_1$ 空间占用代价设计；因目标位姿是常量，不需要逐步变化的 `StageParameters::p`，无需扩展 `third_party/StcSQP` 接口、无需状态增广、无需改动走廊，详见 `docs/NMPC.md` 6.10 节 |
| 2026-07-15 | `tool/tune_post_processor.cpp`：`TuneVariant` | 新增 `global_target_position_weight`/`global_target_heading_weight`/`interior_speed_weight` 三个字段并接入 `RunSingleDataset`；`BuildVariants()` 从单一 `"base"` 变体扩展为 14 组权重扫描变体 | 支撑一轮四数据集广泛调参，验证得到跨数据集安全的默认权重 0.001，详见 `docs/NMPC.md` 6.10.4 节 |
| 2026-07-20 | `BlockTridiagonalSolver` | 从「待实现：ALM 模块核心接口」表迁移至「已冻结接口清单」并冻结：固定 6x6 块 Thomas 求解器首次实现落地（`factorize`/`solve`/`solveTranspose`，正向与转置共享同一次块 LU 消元） | 首次实现经评审确认接口与设计一致；评审意见指出"已实现接口留在规划表中会误导后续 Milestone 的 Dev Agent"，按意见迁移 |
| 2026-07-20 | `MincoTrajectory` / `MincoBoundaryCondition` / `MincoBoundaryCondition2d` | 从「待实现：ALM 模块核心接口」表迁移至「已冻结接口清单」并冻结：原生二维 θ-s 单类 + 边界条件结构体的实际类型划分；K(T) 块行布局保证全部主对角块非奇异，内部航点在 b 中出现两次，终点位置固定位于 b 第 6M-3（0 基）个位置（与设计文档伴随下标公式一致） | 同上 |
| 2026-07-20 | `BicycleKinematicsExtractor` | 从「待实现：ALM 模块核心接口」表迁移至「已冻结接口清单」并冻结：θ-s → 阿克曼状态/控制量映射（`extract`/`steerGradients`/`evaluatePenalties`），δ/δ̇ 含 ε_g 分母正则化且值与梯度同一公式，四约束为防奇异二次/交叉乘积形态、惩罚取 max(0,C)³ 未加权 | 首次实现经评审确认接口与设计一致，按评审意见迁移（同上一条目的处理惯例） |
| 2026-07-20 | `ThetaSSample` / `AckermannState` / `SampleGradient` / `SteerGradients` / `ConstraintPenalty` / `PhysicalConstraintPenalties` / `BicycleKinematicsConfig` | 随 `BicycleKinematicsExtractor` 一并迁移冻结：输入/输出/配置结构体的实际类型划分 | 同上 |
| 2026-07-20 | `docs/ALM.md` 2.2 节 | δ 公式旁补充工程实现说明：工程实现中 δ 同样施加 ε_g 分母正则化（atan(L·θ̇·ṡ/(ṡ²+ε_g))），ε_g→0 时退化为原公式，避免 (ṡ,θ̇)=(0,0) 处 0/0 NaN | 首次实现评审的 📝 建议：设计文档与实现保持一致 |
| 2026-07-20 | `AlmManeuverSegmenter` | 从「待实现：ALM 模块核心接口」表迁移至「已冻结接口清单」并冻结：换挡打断直接消费 `Path` 已有方向切分，微观段按 M=⌈L/d_seg⌉、K_step=⌊(N-1)/M⌋ 抽取锚点（末锚点强制为路径终点），朝向沿路径 2π 解缠绕，累积弧长按方向带符号跨 Maneuver 连续累积 | 首次实现经评审确认接口与设计一致，按评审意见迁移（同前两条目的处理惯例） |
| 2026-07-20 | `ESDFMap` | 底层存储与插值计算从 float 切换为 double（`PhysicalDist` 类型别名、`EsdfCell`、`BilinearParams` 插值权重、批查询参数结构、AVX2 SIMD 开方路径同步切换为 `__m256d`），**公开接口签名不变**（`getDistAndGrad`/`getDist` 返回类型原本即为 `std::pair<double, Eigen::Vector2d>`/`double`），下游调用方无需任何改动 | ALM 路径数值梯度对拍需要 1e-6 量级精度，float 量化噪声（~查询坐标×6e-8）把有限差分验证限制在 ~1e-5 且两点中心差分物理上无法达到 1e-6；实测 double 化查询性能持平、构造慢 15~25%（1000² 缓存压力下最坏 +40%）、内存翻倍，精度收益大于代价，经人工确认切换 |
| 2026-07-20 | `AlmSegmentEstimate` / `AlmManeuverEstimate` / `AlmManeuverSegmenterConfig` | 随 `AlmManeuverSegmenter` 一并迁移冻结：输出/配置结构体的实际类型划分 | 同上 |
| 2026-07-20 | `AlmEsdfPenalty` / `AlmEsdfPenaltyConfig` / `AlmEsdfPoseCost` | 从「待实现：ALM 模块核心接口」表迁移至「已冻结接口清单」并冻结：经评审收敛（🚨=0 ⚠️=0）后正式关闭；`margin_safe`/`margin_comf` 双重三次光滑外点罚 $\Phi(C)=\max(0,C)^3$ 与 θ 旋转链式法则经手推与五点中心差分数值梯度双重验证 | 首次实现经评审收敛后正式关闭 |
| 2026-07-20 | `AlmPreprocessor` / `AlmPreprocessorConfig` / `AlmPreprocessorProblem` / `AlmPreprocessorResult` | 从「待实现：ALM 模块核心接口」表迁移至「已冻结接口清单」并冻结：经评审收敛（🚨=0 ⚠️=0）后正式关闭；$\mathcal{J}_{pre}$ 梯度链路（基函数行 → $K(T)^{-T}$ 伴随 → 航点/s_f + K(T) 行对 T 解析依赖 → τ 重参数化）经全变量有限差分 1e-6 验证；辛普森节点数据共享实现消除梯度/指标不一致风险 | 首次实现经评审收敛后正式关闭 |
| 2026-07-21 | `AlmSolver` / `AlmSolverConfig` / `AlmSolverProblem` / `AlmSolverResult` / `AlmMultiplierState` / `AlmCostBreakdown` / `AlmOuterIterationRecord` | 从「待实现：ALM 模块核心接口」表迁移至「已冻结接口清单」并冻结：经评审收敛（🚨=0 ⚠️=0）后正式关闭；内层代价梯度链（跃度闭式 + ESDF + ALM 终端项）经全变量有限差分 1e-6 验证；λ/ρ 更新与 ρ^0 标定公式逐轮对拍 | 首次实现经评审收敛后正式关闭 |
| 2026-07-21 | `AlmManeuverMelter` / `AlmManeuverMelterConfig` / `AlmMeltResult` / `AlmManeuverMeltInfo` / `AlmMeltClass` | 从「待实现：ALM 模块核心接口」表迁移至「已冻结接口清单」并冻结：经评审收敛（🚨=0 ⚠️=0）后正式关闭；融化判据与 `topology_cleaner` 第一遍判据结构一致（两阈值三分类），修剪复用 `ReconstructPath`，首尾段保护/PIVOT 保留/终点不扰动/方向相反段不合并红线全部经 9 个单元测试验证 | 首次实现经评审收敛后正式关闭 |
| 2026-07-20 | `MincoTrajectory` | `DerivativeBasisRow`（归一化时刻处实时间导数基函数行）从 protected 提升为 public，行为与签名不变 | J_pre 预处理粗优化装配代价对多项式系数的解析梯度时需要复用同一基函数公式；该方法是纯静态函数、不触碰内部状态，公开可避免下游重复实现导致公式发散（与 2026-07-07 `BSplineSmoother::buildKnotVector`/`computeBasisAtU` 公开同理） |
| 2026-07-21 | `PostProcessor` / `AlmConfig` | 新增 ALM 路径入口 `PostProcessor::optimizeAlm()`（Path → AlmManeuverSegmenter → AlmPreprocessor → AlmSolver → AlmManeuverMelter），与既有 NMPC 路径并列且互不干扰；新增 `AlmConfig` 聚合 ALM 五阶段配置与纵向速度上限；新增 `protected` 静态方法 `DeriveKinematicsConfig` 按 `VehicleParams` 派生运动学配置；碰撞质量门常量 0.02m 提升为两条路径共享；`PostProcessor`/`AlmConfig` 同步登记进「已冻结接口清单」 | 端到端集成：把已冻结的 ALM 模块接入既有后处理器，与仓库"实现并比较不同后处理算法"的定位一致；接入方式按规划表约定由 Dev Agent 依据当时实际接口确定 |
| 2026-07-21 | `AlmEsdfPenaltyConfig::weight_safe` | 默认值从 1.0 改为 100.0（接口签名不变，仅默认值校准） | 四数据集首次端到端验收实测：1.0 时三次光滑外点罚在 C→0 处梯度消失（∝C²），软惩罚平衡态侵入 0.029~0.072 m，超出 0.02 m 碰撞质量门；按梯度平衡 C∝1/√W 标定到 100.0 后平衡态压回安全侧约一个量级，四数据集全部满足质量门且对已通过的 data1/data7 无负面影响，详见 [docs/ALM.md](ALM.md) 3.3 节 |
| 2026-07-21 | `PostProcessorResult` / `AlmConfig` / `ALMPlanningScene`（新增） | `PostProcessorResult` 新增 `alm_traj` 字段（ALM 输出轨迹，采样点未携带时间戳，供可视化与下游消费）；`AlmConfig` 改为继承通用 `Config` 基类（签名兼容的结构性扩展，与 `NMPCConfig` 既有模式一致）；新增 `src/scene/alm_planning_scene.h/.cpp`（效仿 `NMPCPlanningScene` 的 ALM 场景类）；`main.cpp` 同时执行 NMPC 与 ALM 并生成三条轨迹对比图 | 人工明确提出生产入口可视化需求：在 main 中读取数据文件分别执行两条路径优化并展示效果；`AlmConfig` 继承 `Config` 是场景基类 `std::unique_ptr<Config>` 统一管理配置的前提 |
| 2026-07-21 | `AlmManeuverMelter::detectMelting` | 首尾段保护语义细化（接口签名不变，仅分类行为修正）：首/末 Maneuver 在原有"不因融化判据被剔除（MELTED）"基础上，新增"不被重分类为 PIVOT"豁免，无论判据量如何均只分类为 NORMAL | 全局质量校验的 ⚠️ 级发现：末段被重分类为 PIVOT 时，`meltAndPrune` 的位置压平会把输出终点从 PHR-ALM 收敛值挪到该段首点（偏差量级达 `melt_arc_threshold` 默认 0.05m），违反 `docs/ALM.md` 2.6.3 节"微调必须保持已收敛的终点 x,y,θ 等式约束不被扰动"的设计契约，导致合法收敛解被终点质量门无谓拒绝；新增 `FirstAndLastManeuverNeverPivot` 单元测试钉住该行为 |
| 2026-07-22 | `AlmManeuverMelter::meltAndPrune` | PIVOT 处理语义修正（接口签名不变，仅行为修正）：彻底移除 `PIVOT` 分支的采样点位置压平（$x,y$ 压平到段首点）与 $v,a$ 清零逻辑，`PIVOT` 收窄为纯分类标签——采样点 $x,y,v,a,\delta,\dot\delta$ 与 `NORMAL` 段完全同等对待、不再做任何改写；`direction` 仍改写为 `Direction::PIVOT`（选择保留标签而非保持原方向：`ReconstructPath` 对该方向既不剔除也不合并，保留这段"净位移极小、朝向变化不小"微动机动的拓扑独立性与 `pivot_count`/可视化等下游观测能力，且改动面最小） | 旧处理在 $v\equiv0$ 时保留 $\theta,\delta$ 变化，与全链路唯一承认的运动学关系 $\dot\theta=v\tan\delta/L_{base}$ 自相矛盾（分析见 [docs/known-limitations.md](known-limitations.md) 对应条目与 [docs/ALM.md](ALM.md) 3.4 节）；修复：`PivotSegmentIsPreserved` 重写为与对照组逐位一致的数据不改写校验，新增 `PivotSegmentSatisfiesBicycleKinematics` 钉住 $\dot\theta\approx v\tan\delta/L_{base}$ 运动学一致性 |
| 2026-07-22 | `SampleMincoTrajectory` / `CheckMincoSampleStructure`（新增）/ `AlmManeuverMelter` | 新增独立"MincoTrajectory → 采样 Maneuver 序列"离散化工具并登记进「已冻结接口清单」：形态定为 `src/core/ALM/alm_trajectory_sampler.h` 下的独立自由函数（而非 `AlmManeuverMelter` 公开方法）——离散化不依赖融化阈值等任何 melter 配置（采样密度显式传参），纯函数无内部状态、天然满足"两次调用互不干扰"，且不引入新类，符合最小侵入原则；`AlmManeuverMelter` 的 `protected` 方法 `sampleToManeuvers`/`CheckStructure` 随之移除（均为非公开成员，不影响既有公开冻结签名），`meltAndPrune`/`detectMelting` 改为调用该工具，对外行为不变、既有全部测试保持通过 | 需要对 `AlmPreprocessorResult::trajectory`（优化前）与主优化收敛轨迹（优化后）做同一套离散化以生成可比较的 `Trajectory`；该能力此前耦合封装在 `AlmManeuverMelter` 内部且为 `protected`，按 spec 要求拆出为不依赖融化/剔除逻辑的独立可复用工具 |
| 2026-07-22 | `PostProcessorResult` / `ALMPlanningScene` | `PostProcessorResult` 新增 `alm_preprocessed_traj` 字段（ALM 预处理粗优化轨迹的离散化结果，"优化前"对比基线，采样点未携带时间戳；预处理失败或不执行 ALM 路径时为空）；`optimizeAlm` 在预处理成功后立即用 `SampleMincoTrajectory`（与最终输出同一采样密度、同一运动学提取器）离散化填充；`ALMPlanningScene` 新增 `almPreprocessedTraj()` 只读访问器与对应成员，风格与既有 `almTraj()` 一致 | 为 `main.cpp` 对比图提供"优化前"基线，取代此前"运行一次完整 NMPC 求解只为取其预处理管线产出"的做法；接入点选择 `PostProcessor::optimizeAlm` 内部填充 + 场景层只读转发，与 `alm_traj` 的既有数据流完全同构 |
| 2026-07-22 | `src/main.cpp` | 移除 `NMPCPlanningScene` 的构造与 `optimize()` 调用（NMPC 路径本身保留、可独立调用），端到端只构造并执行 `ALMPlanningScene`；对比图两条曲线改为 "ALM Preprocessed"（红，`almPreprocessedTraj()`）与 "ALM Optimized"（绿，`almTraj()`），地图/车辆参数均取自 ALM 场景 | 目标：同一 θ-s 管线内部的"优化前/优化后"同族对比，消除"为拿一条参考曲线白付一次 NMPC/HPIPM 求解"与双场景重复构建环境数据的开销，去除 ALM 生产入口对 NMPC 场景的隐性依赖 |
| 2026-07-22 | `NmpcSolver::ToPath` / `PostProcessor::runSingleAttempt`（行为修正，签名不变） | 修复状态增广（7 维，`BicycleModelJerk`）结果回填 `TrajectoryPoint` 时 `a`/`delta_dot` 张冠李戴的问题：增广模型中 `a`/`delta_dot` 是状态分量（索引 5/6）、每个采样点都有真值，控制序列承载的是 `jerk`/`ddelta_dot`，旧代码却一律从控制序列回填（把 jerk 填进 a、把转向角加速度填进 delta_dot）；现按状态维度分支：7 维从状态分量回填（含各段末点），5 维保持控制序列回填（末点无控制量） | 状态增广重构（2026-07-15 条目）只改了 OCP 装配侧，两处输出回填未同步更新；该错误直接污染 `Trajectory::validate()` 运动学校验的 v/δ 残差输入（Δv 对 jerk 的梯形残差毫无意义），是后续运动学可行性校验容差标定的前置修复 |
| 2026-07-22 | `VehicleFootprintModel` | 新增 `getWheelbase()` 只读访问器（返回构造时 `VehicleParams` 的轴距） | `Trajectory::validate()` 的运动学校验需要运动学关系 θ̇=v·tanδ/L 中的轴距 L，而 `validate()` 的既有签名只携带 footprint 模型；与 2026-07-07 `getOuterRowNum()` 同类的只读补充 |
| 2026-07-22 | `TrajectoryValidationConfig` / `TrajectoryValidationResult` / `Trajectory::validate` | 新增运动学可行性校验（第三门）：`validate()` 对相邻两点检验 ẋ=v·cosθ、θ̇=v·tanδ/L、v̇=a、δ̇=δdot 的梯形配点残差；Config 新增 `max_kinematic_dt`（0.5s）与四项残差阈值，Result 新增 `kinematic_feasible`、四项最大残差与 `kinematic_detail`；`all_passed` 扩展为三门合取；时间戳缺失/无可评估点对时该门不证伪（记 true 并注明 skipped），空轨迹记 false。**容差标定依据（三数据集 NMPC 优化结果实测，可复现：`test/trajectory_kinematic_validation.t.cpp` 的标定回归用例打印 `[KIN-CAL]`）**：① Δt 分布——Δt≤0.5s 的点对占 83%~93%（data3 213/240、data1 144/174、data7 205/221），Δt>0.5s 的点对全部位于换挡停驻补丁（v≈0），其残差由 O(Δt³) 截断主导（实测最大 0.434 m / 4.36°，与可行性无关），故设 `max_kinematic_dt=0.5`；② Δt≤0.5s 点对实测最大残差——位置 0.0060 m（data3；data1 0.00082、data7 0.0032）、航向 1.526°（data3；data1 0.018°、data7 0.053°）、速度 ~1e-15 m/s、转角 ~1e-15 rad（后两项因 a/δ̇ 为状态量、控制分段恒定时梯形积分精确）；③ 阈值取值——位置 0.02 m（3.3 倍余量）、航向 3.0°（2.0 倍余量）、速度 0.05 m/s、转角 0.02 rad（后两项相对机器精度级残差为宽余量）；④ 反向验证——已知不可行合成轨迹全部被拒绝：位置跳变 0.5 m、v≡0 但逐步位移 0.1 m、δ=0.3 rad 但 θ 不变（Δt=0.5s，残差 3.28°）、v 瞬时跳变 1.0 m/s、δ 瞬时跳变 0.3 rad | 落地动因：既有校验只覆盖碰撞安全与终点收敛，完全不检查轨迹自身是否满足阿克曼运动学（PIVOT 自相矛盾类缺陷无任何门禁）；校验只消费 `TrajectoryPoint` 自带量与通用运动学关系，`util::Trajectory` 不新增对 `core/NMPC`/`StcSQP` 的依赖 |
| 2026-07-22 | `TrajectoryValidationConfig` | 新增 `kinematic_low_speed_epsilon`（默认 0.05 m/s）：相邻两点 \|v\| 均低于该值时跳过前轮转角残差（δ̇=δdot）评估。标定依据：ALM 四数据集调参实测发现，θ-s 参数化下换挡尖点（ṡ→0）附近 δ=atan(L·θ̇·ṡ/(ṡ²+ε_g)) 可在 atan 值域（±π/2）内跳变，产生 ~π 的转角残差——但近零速度下 θ̇=v·tanδ/L≈0 与 δ 取值解耦，δ/δ̇ 不承载运动可行性信号，该残差是奇异特征处的截断伪影而非可行性证据（同批次实测位置/航向/速度残差仅 1e-14 m / 0.13° / 1.5e-5 m·s⁻¹）。位置/航向/速度残差在低速下仍照常评估，"v≡0 但 θ 变化"类矛盾仍由航向残差检出；行驶速度下（\|v\|≥0.05）的 δ 跳变仍被正常拒绝（`SteerFlipAtDrivingSpeedIsRejected` 钉住） | ALM 调参实测：运动学门对 ALM 产出直接应用时，换挡尖点转向跳变造成全数据集假阳性（kin_steer≈3.14 rad）；该修复是调参验收的前置，且保持 NMPC 侧三数据集标定回归行为不变（其转角残差本就 ~1e-15，跳过低速点对不改变结论） |
| 2026-07-22 | `SampleMincoTrajectory` / `PostProcessorResult::alm_traj` / `alm_preprocessed_traj` | 离散化产出的采样点新增时间戳（`setT`，取 θ-s 轨迹全局时刻，含 `AlmManeuverMelter` 输出 `Path` 与两个 `Trajectory` 视图）；接口签名不变，行为增量扩展 | θ-s MINCO 轨迹本身是时间参数化的多项式（段时长是优化决策变量），采样点携带真实时刻后 `Trajectory::validate()` 的运动学可行性校验（梯形配点残差依赖 Δt）才对 ALM 产出真正生效——此前"采样点未携带时间戳"会使该门真空跳过，后续 ALM 调参的"三项合法性"验收将形同虚设 |
| 2026-07-22 | `AlmSolverConfig::weight_jerk_s` / `weight_gear_cusp` | `weight_jerk_s` 默认值 1.0 → 5.0；`weight_gear_cusp` 默认值 1000.0 → 50.0（接口签名不变，仅默认值校准）。**标定依据（四数据集 25 组变体系统扫描，可由 `tool/tune_alm.cpp` 复现）**：调参前默认在 data1/data6 上长度已劣于既有基线（7.23>7.05、29.83>28.23 m）；单变量扫描确认 `weight_jerk_s` 为段数/长度主杠杆（1→5 双降，≥10 时 data6 段数回升到 6），`weight_gear_cusp` 放宽有助于换挡邻域融化（1000 过死、5000 过宽均恶化），`weight_jerk_theta`/`epsilon_time`/melter 阈值均被实测否决；最终组合（5.0/50.0）四数据集全部满足 `Trajectory::validate()` 三门（运动学残差 1e-14 m~1e-3 rad、碰撞全零、终点 ≤0.038 m），段数 ≤ 基线（3/2/2/4）、长度全面低于基线（11.77/6.98/13.87/27.80 m） | ALM 调参验收要求"合法前提下削减机动段数与路径总长度、相对既有基线不劣化"；该组合是全部合法变体中终点余量最安全（data3 0.038 m vs 次优 0.042 m）且碰撞全零者 |
| 2026-07-22 | `BicycleKinematicsExtractor` / `BicycleKinematicsConfig` | `C_δ`（前轮最大转角约束）的惩罚形态从 max(0,C)³ 改造为分段 C¹ 光滑 hinge（C≤0 恒为 0；0<C≤ε_h 取 C²/(2ε_h)；C>ε_h 取 C−ε_h/2、梯度 1·∇C），新增配置字段 `steer_hinge_epsilon`（默认 1e-4）；`C_v`/`C_a`/`C_δ̇` 保持 max(0,C)³ 不变。**依据**：三次形态在小违反区梯度 ∝C² 消失，换挡尖点邻域的转向角违反（实测 C~1e-3、惩罚 ~1e-9）对优化器不可见，是 κ=tanδ/L 尖峰（峰值 28~170）的直接根因；hinge 形态在违反区梯度不消失。新增 `MakeHingePenalty` 静态方法，值与解析梯度经中心差分对拍验证 | 换挡曲率补救方向 2；单纯加重权重（方案 1，1e4~1e6 扫描）实测无效且破坏段数/长度/预处理收敛，形态改造是唯一能压住所述违反的机制 |
| 2026-07-22 | `AlmPreprocessorConfig` / `AlmSolverConfig` | 新增 `weight_gear_cusp_theta` 字段（默认 200.0）：换挡航点 θ̇² 点态软惩罚（与既有 ṡ² 换挡惩罚同位置同结构），把"换挡瞬间停转"写入代价，消除换挡点 θ̇≠0 且 ṡ→0 导致的 κ 尖峰；`weight_steer_angle` 默认值调整：预处理 1000.0→**0.0**、主求解 1000.0→**15.0**。**标定依据（30+ 组权重组合扫描）**：hinge C_δ 有效权重下预处理与前端路径（κ~0.186 超上限 ~8%）冲突导致收敛失败，故预处理关闭 C_δ；主求解 15.0 配合 weight_gear_cusp_theta=200.0 与 weight_safe=600.0 的组合使四数据集行驶速度最大 κ 压到物理上限 ~1.00~1.03 倍、|δ̇|≤0.38 rad/s、段数不增于原始路径、长度缩短 11~19% | 方向 3 落地；梯度链路（点态惩罚 + K(T) 伴随 + 显式时长项）与 ṡ² 惩罚同构，经全变量有限差分对拍验证 |
| 2026-07-22 | `AlmEsdfPenaltyConfig::weight_safe` | 默认值 100.0 → 600.0（接口签名不变，仅默认值校准）。标定依据：合法化组合（hinge C_δ weight_steer_angle=15.0）会压缩 ESDF 惩罚的相对权重，data3 碰撞回升至 0.024 m 超 0.02 m 质量门；600.0 使四数据集碰撞 ≤0.008 m 且不劣化段数/长度 | 与上行权重组合的配套标定，扫描覆盖 100/200/400/600/800 |
| 2026-07-22 | `ApplySteerPadding` / `AlmSteerPaddingConfig` / `AlmSteerPaddingStats`（新增） | 新增停驻窗口合法化后处理模块（`src/core/ALM/alm_steer_padding.h/.cpp`），并登记进「已冻结接口清单」：对 |v|<v_epsilon（0.05 m/s）的连续停驻窗口，净 Δθ ≤ max_freeze_dtheta（0.02 rad）时做"低速蠕行打轮"改写——θ 冻结为窗口首点值（消除 pivot 旋转），δ 从窗口前最后一个驱动点的限幅值按 ≤δ̇_max 斜率向后延伸过渡直至与原 δ 剖面相交（斜率继承保持相邻窗口 δ̇ 连续），x,y/v/a/t 保持原值；净 Δθ 超阈值的真实 pivot 旋转窗口保持原样（阿克曼车辆无原地转向能力，需多点掉头）。`PostProcessor::optimizeAlm` 在机动融化后、质量门前调用 | θ-s 换挡邻域停驻窗口内轨迹表现为 θ̇≠0 且 δ 在 atan 值域内翻转（物理不可执行，κ 峰值达 28~170），是 κ 绘图尖峰与"max κ>0.5"投诉的直接来源；该后处理把伪影窗口合法化为"低速蠕行中调整前轮"，逐对梯形残差保持为零 |
| 2026-07-22 | `TrajectoryValidationConfig::max_kinematic_steer_residual` | 默认值 0.02 rad → 0.05 rad（ALM 轨迹族重标定）。依据：NMPC 丝滑轨迹实测残差 ~1e-15（原标定基础）；hinge C_δ 压出的"合法但急转"ALM 轨迹在急转向区的梯形配点残差实测 0.023~0.030 rad——硬件上限已单独验证（|δ|≤1.04×δ_max、|δ̇|≤0.38 rad/s），该残差是配点离散伪影而非不可行证据；0.05 取约 2 倍余量，对真实 δ 跳变（0.3~3.0 rad）仍保有一个量级以上检出余量 | 原阈值以 NMPC 轨迹族标定，未覆盖 ALM 合法急转轨迹族；与换挡曲率补救配套 |
| 2026-07-23 | `DetailSeriesData` / `Visualizer` | `DetailSeriesData` 移除 5 个死字段：`delta_t`/`slack_true`/`slack_calc`（从未写入读取，仅 reserve）、`time`（写而不读，无任何子图以其为轴）、`lateral_d`（写而不读，调用方恒传 0.0）；`appendDetailPoint` 同步去掉 `lateral_d` 参数。冻结清单中 `DetailSeriesData` 条目同步更新为当前真实字段集；`Visualizer` 条目重写——原条目登记的 `plotPipelineDiagnostics()`/`drawDetailSubplotDualY()` 在代码中已不存在（管线诊断功能此前的重构中移除，文档未同步），现按代码实况登记三类画布职责与刻度算法 | visualizer.hpp 彻底重构（死代码清理）：零调用符号清点确认上述字段无任何消费方；文档失配条目一并修正，保持冻结清单与代码一致 |
| 2026-07-23 | `Trajectory` | 新增构造函数 `Trajectory(const Path&, const VehicleParams&, double ref_speed = 1.0)`：以恒定参考速率 \|v\|=ref_speed 为时间律，按自行车模型微分平坦关系补全几何路径缺失的运动学量——v=σ·ref_speed（σ 由 Maneuver 方向决定，BACKWARD 取 -1、其余取 +1）、a≡0、运动方向签名曲率 κ=σ·κ_geom（与 tanδ/L 及轨迹对比视图约定一致）、δ=atan(L·κ)、δ̇ 由 δ 对 t 的段内差分给出、t 按弧长/ref_speed 累积；未 finalize 路径缺失的 κ 按 0 处理，换挡边界重复点按 `Path::forEach` 语义跳过；`ref_speed`/`wheelbase` 非正或非法时抛 `std::invalid_argument`。既有全部签名不变 | main.cpp 对比图由"预处理轨迹 vs 优化轨迹"改为"原始路径 vs 优化轨迹"后，原始 `Path` 只携带 x/y/θ/κ 几何量，无法直接进入 `plotTrajectory` 的同族信号（v/a/δ/δ̇）对比；该构造函数把纯几何路径补全为全量参考轨迹，是可视化对比的最小侵入式支撑 |
| 2026-07-23 | `Trajectory` / `TimeLaw` 族（新增）/ `MakeSpeedProfileTimeLaw`（新增） | `Trajectory` 新增 `(const Path&, const VehicleParams&, const TimeLaw&)` 重载：几何/微分平坦量（κ=σ·κ_geom、δ=atan(L·κ)、δ̇=dδ/dt 段内差分）仍由构造侧补全，纵向时序量（v/a/t）改由时间律策略注入；各非末机动段的末个发射点作为换挡点写入 `TimeLawInput::cusps`；时间律不可调用或输出长度不匹配抛 `std::invalid_argument`。原 `double ref_speed` 重载保留，语义不变（委托 `MakeConstantSpeedTimeLaw`）。新增 `src/util/time_law.h/.cpp` 与 `src/preprocessing/speed_profile_time_law.h/.cpp` 两个模块并登记进「已冻结接口清单」：前者提供 `TimeLaw` 类型与恒定速率/bang-bang 梯形加减速两种内置时间律；后者复用 NMPC 预处理 `SpeedProfilePlanner` 按 `plan()` 契约逐机动段求解再拼接，ESDF 减速项惰性化，并以"逐区间取梯形估计与匀加速估计较小者"的全局时刻重构修复 s 域 QP 段首 `a_0=0` 边界导致的零速停驻伪影（修复前实测 10m 直线 duration 达 110s，修复后 11.3s）| 人工要求原始路径的时间推断改采"最快走完"前提（首末/换挡点零速），并同图对比两种方法：自构思 bang-bang 梯形加减速（data7 实测 19.6s）与 NMPC 预处理 QP（41.2s，jerk 平滑代价）；恒定速率假设无法表达换挡停驻与加减速约束，策略化注入使两种时间律与 `Trajectory` 构造侧解耦 |
| 2026-07-23 | `Trajectory` / `TimeLaw` 族（删除）/ `MakeSpeedProfileTimeLaw`（删除） | 按人工决策收敛时间推断方案：保留 bang-bang 梯形加减速为唯一时间参数化，删除 `TimeLaw` 策略抽象（`TimeLaw` 类型/`TimeLawInput`/`TimeLawOutput`、`MakeConstantSpeedTimeLaw`、`MakeTrapezoidalTimeLaw` 工厂、恒定速率 `double ref_speed` 构造重载）与 `MakeSpeedProfileTimeLaw` QP 适配器（`src/util/time_law.h/.cpp`、`src/preprocessing/speed_profile_time_law.h/.cpp` 两个模块整体删除）；`Trajectory` 的 Path 构造函数收敛为 `(const Path&, const VehicleParams&, const TimeProfileConfig& = {})`，内部调用新模块 `src/util/time_profile.h/.cpp` 的 `ComputeTimeProfile`（原梯形律实现平移为普通函数 API，算法与行为不变，并补输入长度/换挡点越界校验）；冻结清单同步以 `TimeProfileConfig`/`TimeProfile`/`ComputeTimeProfile` 条目替换 `TimeLaw` 族与 `MakeSpeedProfileTimeLaw` 条目 | 同图对比结论：bang-bang 梯形律（data7：19.55s）在"最快走完"语义下优于预处理 QP 律（41.19s）且实现简单、无 s 域零速伪影，人工选定其为唯一方案；策略抽象失去多实现支撑，按 YAGNI 删除 |
| 2026-07-23 | `PlanningScene::init` / `data/config.json` / `apa_tune_alm` | 场景配置新增可选字段 `outer_row_num`（外圆行数，缺失默认 2，与改动前生产行为一致）：`PlanningScene::init` 读取后传入 `VehicleFootprintModel` 构造，沿"场景 footprint 模型 → `PostProcessor` → `AlmEsdfPenalty`（OUTER 圆）"链路透传到 ALM/NMPC 两条算法的真实碰撞模型，加载日志同步打印实际生效值；`data/config.json` 显式补齐 `"outer_row_num": 2`。`tool/tune_alm.cpp` 的 footprint 构造同步改为从 `data/config.json` 读取同一字段——**修复一处既有一致性缺陷**：调参工具此前使用 `VehicleFootprintModel` 声明默认值 `outer_row_num=4`，与生产场景的 2 不一致，此前的历史调参评价（碰撞深度等指标）是在不同碰撞模型下得出的；接口签名均不变，行为增量扩展 | 人工要求外圆行数可从外部配置并透传到真正实现；外圆行数是碰撞模型的场景级参数（非算法调参项），放在场景级 `config.json` 而非算法配置详情文件；调参与生产必须同源，否则调参结论不可迁移 |
| 2026-07-23 | `Config` / `PlanningScene` / `ALMPlanningScene` / `NMPCPlanningScene` / `LoadBaseConfigOverrides`（新增）/ `data/alm_config.json`（新增） | **"每算法一个配置详情 JSON"约定落地**（取代同日早先的 `config.json` 直挂 `outer_row_num` 字段方案）：① `Config` 基类碰撞安全组新增 `outer_row_num` 字段（默认 4）；② `PlanningScene::init` 调整顺序——先 `loadConfigDetails` 再构造 footprint 模型（后者依赖 `config_->outer_row_num`），新增静态工厂 `LoadFromFile`（读 `config_details_path` 指向的详情 JSON 的 `"algorithm"` 字段路由到 ALM/NMPC 场景，文件缺失/字段缺失/算法未知返回 nullptr）与 `optimizedTraj()`/`algorithmName()` 两个纯虚接口；③ 新增 `src/util/config_loader.h/.cpp` 的 `LoadBaseConfigOverrides`（解析算法无关基类字段覆盖项，当前映射 `outer_row_num`，供场景层与调参工具共用防漂移），`ALMPlanningScene::loadConfigDetails` 由空实现改为调用它；④ 新增 `data/alm_config.json`（`{"algorithm": "alm", "outer_row_num": 2}`，保持既有生产行为），`data/nmpc_config.json` 补 `"algorithm": "nmpc"`（proto 解析忽略未知字段，`outer_row_num` 未覆盖即取基类默认 4——**NMPC 碰撞模型因此由 2 变为 4，如需保持旧行为在该文件显式配置 2**），`data/config.json` 移除直挂字段；⑤ `main.cpp` 改为 `PlanningScene::LoadFromFile` 运行时场景选择 + 默认绘制"初始轨迹（红）vs 优化轨迹（绿，标签取 `algorithmName()`）"，优化失败只画初始轨迹——切换算法只需改 `config_details_path`；⑥ `apa_tune_alm` 的 baseline 变体改为从 `data/alm_config.json` 加载基类覆盖项，footprint 外圆行数与变体配置同源 | 人工明确要求：碰撞安全参数进 `Config` 基类、每算法一个配置详情 JSON、生产入口按 `config_details_path` 运行时路由场景、失败只画初始轨迹，免除"对比 NMPC/ALM 需改 main 编译两次"的开销 |
| 2026-07-23 | `PlanningScene` / `ALMPlanningScene` / `NMPCPlanningScene` | 场景成员与访问器命名收敛到算法无关语义：基类成员 `nmpc_traj_` 更名 `optimized_traj_`、基类访问器 `nmpcTraj()` 删除（其语义已由此前新增的纯虚 `optimizedTraj()` 承载）；`NMPCPlanningScene::optimizedTraj()` 改返 `optimized_traj_`；`ALMPlanningScene` 的 `[[deprecated]] nmpcTraj()` 隐藏方法同步移除（基类已无该接口，无需防误用）。新增 `optimizeSummary()`（生成优化摘要字符串：成功时含优化前后路径长度/机动段数变化/耗时，未执行或失败时注明原因与耗时、不含优化后指标）与 `printOptimizeSummary()`（成功记 LOG_INFO、失败记 LOG_ERROR），`main.cpp` 在 `optimize()` 后调用后者 | 人工评审意见：`nmpcTraj` 命名在算法无关基类上已不再合理；生产入口需要统一的优化摘要打印（含失败处理），免除各调用点自行拼装 |
| 2026-07-23 | `AlmManeuverSegmenterConfig` / `AlmManeuverSegmenter` / `ApplySteerPadding` / `AlmSolverConfig` / `AlmPreprocessorConfig` | **段数压缩调参与重构**（验收标准经人工修订为"合法硬前提 + 长度 ≤ 原始路径 +10% 内尽可能压缩机动段数"）：① `AlmManeuverSegmenterConfig` 新增 `fuse_arc_threshold`（微段融合弧长阈值，默认 **1.5 m**，0 为关闭）与 `fuse_heading_threshold`（朝向阈值，默认 0.2 rad）；`AlmManeuverSegmenter` 新增 protected 方法 `FuseShortManeuvers`——内部方向为 FORWARD/BACKWARD、|Δs| 与 |Δθ| 均低于阈值的摆动微段在分段阶段即被移除（邻段弧长平移重锚保持 θ-s 链连续、同向邻段合并），首末段绝对保护、PIVOT/UNKNOWN 永不融合；② `ApplySteerPadding` 新增交界平滑：对改写区域（含左侧邻点）做 3 点滑动平均并从平滑后 δ 差分重算 δ̇——线性斜坡与原剖面交界的 C¹ 折角在爬行区长 dt 点对下曾把转向梯形配点残差放大到 0.059（>0.05 门），折角分摊后四数据集 kin_steer 压到 0.007~0.031，基线自身同步受益（data3 0.039→0.022、data6 0.040→0.016）；③ `AlmSolverConfig::weight_steer_angle` 15.0 → **10.0**；④ `AlmPreprocessorConfig::weight_endpoint_track` 10.0 → **20.0**（融合后 data1 初始结构跨距变大，10.0 预处理终点误差 0.12 m 超容差）。最终四数据集段数 9→7、10→2、6→4、6→4（基线 9/4/4/6），全部合法（kin_steer ≤0.031、碰撞 ≤0.005、行驶 κ ≤ 1.012 倍物理上限、终点 ≤0.034 m），长度缩短 12%~18% | κ 合法化封死 pivot 压缩后优化器无法把摆动段收缩到融化阈值以下（批次 1：13 组单杠杆变体段数全部不变），段数压缩只能前移到分段结构层 |
| 2026-07-23 | `Trajectory` / `PostProcessorResult::final_maneuvers` | **机动段数指标口径统一为物理方向段数（v 变号 + 位移过滤）**：`Trajectory` 新增 `countDirectionRuns(double v_epsilon = 1e-3, double min_arc = 0.05)`——第一遍按明确符号（|v|≥eps）切段并累计各段位移（停驻点不改变方向状态、不产生段边界，位移归入当前段），第二遍丢弃位移不足 min_arc 的抖动段并合并同号邻段（与 `Path::addPoint` 方向推断 ds≥DELTA_DIST 的语义一致）；轨迹首个带符号段无条件保留（初始方向段位移短仅因观测窗口不足）。`PostProcessor::optimize()` 与 `optimizeAlm()` 的 `final_maneuvers` 均由 `Path::numManeuvers()`（容器标签数）改接该口径（对应轨迹缺失的失败/回退分支回退为标签数），字段注释同步更新。最终口径四数据集实测：ALM 9→7、10→4、6→4、6→4；NMPC 9→7、10→8、6→5、6→6；验收段数上限收紧为 8/5/5/5 | 人工指出融合后估计结构的标签机动段数与物理换挡次数不一致（data1 标签 2 而倒车段内含 ~0.95m 前向过冲、实际 4 段）会引人误会；MINCO 多项式段内部无单调性约束（仅换挡边界 ṡ≈0 惩罚），标签口径无法覆盖段内过冲。纯速度阈值口径对离散 OCP 轨迹停驻区的 ±cm/s 数值抖动敏感（NMPC 曾被误计为 9→10/10→10 的"段数增多"），故按位移过滤抖动 |
| 2026-07-23 | `NMPCPlanningScene::loadConfigDetails` | **修复基类配置覆盖项被静默忽略的管线缺口**：NMPC 场景的详情加载此前只走 `NMPCConfigProto` 解析（proto 忽略未知字段），`data/nmpc_config.json` 中的算法无关基类字段（`outer_row_num`）从未生效——生产 footprint 外圆行数实际恒为基类默认 4。现与 ALM 场景共用同一入口 `LoadBaseConfigOverrides`（先于 proto 解析应用），配置缺失/加载失败记错误日志并保默认；新增 `PlanningSceneTest.NmpcSceneAppliesBaseConfigOverrides` 钉住（outer_row_num=3 透传为 3）。修复后 `data/nmpc_config.json` 的 `outer_row_num: 2` 实际生效，四数据集 NMPC 生产数字更新为：data3 9→7/20.882m/CONVERGED、data1 10→8/10.594m/CONVERGED、data7 6→5/18.105m/CONVERGED、data6 6→6/36.836m/回退预处理（同次运行 validate 三门全过） | 用户为 NMPC 配置 `outer_row_num: 2` 以保留其历史碰撞模型，缺口使该意图静默落空；2 行外圆（较 4 行更粗）下可解数据集全部完全收敛且显著更快（2.2~5.6s vs 10~17s），但路径更保守、长度收益收窄 |
| 2026-07-24 | `constants.h` / `PreprocessingPipelineConfig` / `BSplineSmootherConfig` / `NMPCConfig` / `IterativeCorridorConstraint` | 移除从未被代码引用的 `kCollisionEpsilon` 常量，其文档语义由既有 `EPSILON_PRECISE = 1e-6` 承接：三个碰撞检测数值容差字段（`PreprocessingPipelineConfig::collision_safety_margin`、`NMPCConfig::collision_safety_margin`、`BSplineSmootherConfig::collision_margin`）与 `IterativeCorridorConstraint::hard_margin` 构造默认值从 **0** 改为 `EPSILON_PRECISE`（接口签名不变，仅默认值变更）；`NMPCConfig::corridor_hard_margin`（0.05，吸收线性化截断误差的物理裕度）、`Config::esdf_safety_margin`（0.1，舒适软代价）、`soft_margin`（0.18）等物理/舒适裕度保持不变。四数据集 × ALM/NMPC 改动前后对照（各 8 次完整运行）：ALM 四数据集结果逐位一致（不经过该代码路径）；NMPC data1/data3/data6 长度/段数/碰撞深度完全一致，data7 的 1e-6 扰动经 L-BFGS/SQP 迭代路径放大，预处理打靶维数 221→220 步、收敛标志由 fully converged 变为 using last iterate，最终长度 18.105→18.099 m、碰撞深度均为 0、段数不变、校验门通过——属非线性优化对浮点级扰动的正常敏感性，无物理行为变化 | 2026-07-10 更新把物理安全裕度统一归零并在文档中承诺以 `kCollisionEpsilon` 兜底浮点误差，但该常量从未落地到代码（全仓库仅定义、零引用），文档与实现不一致；改用既有 `EPSILON_PRECISE` 作为 margin 默认值使该承诺真正生效，且不新增常量 |
| 2026-07-25 | `NMPCConfig::loadFromProto` / `Config` | 修复代价权重双源缺口：`Config` 基类 12 个代价权重字段（`control_effort_*`/`smoothing_*`/`interior_*`/`global_target_*`/`terminal_*`）此前从未被生产路径读取（`PreprocessingToOcpConverter` 只读 `path_to_ocp_config`，proto/JSON 配置被静默忽略）；现 `loadFromProto` 末尾将基类字段无条件同步进 `path_to_ocp_config`（基类为唯一权威来源，接口签名不变）；同时将基类 `smoothing_jerk_weight`/`smoothing_steer_accel_weight` 默认值从 1.0 修正为 1e-1（与 `PathToOcpConfig` 及四数据集历史调参值一致）。四数据集对照实验（1.0 生效 vs 1e-1 生效）：1.0 时 data1 更短（10.268m vs 10.594m）且速度剖面更平滑但段数 8→9、data7 完全收敛且提速约 6 倍但长度 +1.2m、**data3 求解失败回退预处理（不可接受）**、data6 不变；改回 1e-1 后四用例长度/段数与修复前逐位一致，523/523 单测通过 | 用户发现 JSON 配置基类权重被静默忽略的配置缺口；基类 1.0 默认值从未在生产生效过（状态增广重构遗留的死默认值），实验证实 1e-1 是四数据集唯一已验证取值，故以其修正基类默认值并在注释中钉住"勿直接放大" |
| 2026-07-28 | `src/core/DDP/`（新模块，规划中） | 新增"待实现：DDP 模块核心接口"一节，登记预处理/参考构建、七维自行车动力学、box-QP、代价/约束求值层、MS-iLQR 内层、AL 外层、后处理与场景接入等规划中接口的目标头文件路径与设计意图，对应 [docs/DDP.md](DDP.md) 与 [docs/milestones.md](milestones.md) 登记的 DDP 模块开发计划（Milestone 001~009） | 与 [docs/architecture.md](architecture.md) 3.8 节同步：DDP 是与 NMPC/ALM 并列的第三条后处理算法路径，尚未创建任何源文件，先落地接口规划供 Milestone 拆分与后续 Dev Agent 实现参考（沿用 2026-07-19 ALM 规划表惯例） |
| 2026-07-28 | `DdpReferenceBuilder` / `DdpReferenceBuilderConfig` / `DdpReference` / `DdpReferenceManeuver` / DDP 共享基础件（`DDP_STATE_DIM`/`DDP_CONTROL_DIM`/`DDP_IDX_*`/`DdpState`/`DdpControl`/`DdpAlignedVec<T>`/`WrapAngle()`） | 从「待实现：DDP 模块核心接口」表迁移至「已冻结接口清单」并冻结：Milestone 001 经两轮评审收敛（🚨=0 ⚠️=0），正式关闭；重采样/初值提取/cusp 检测/打靶节点布设全部经 12 个单元测试 + 四数据集参数化验收验证 | 首次实现经评审收敛后正式关闭（比照 2026-07-20/21 ALM 规划表迁移惯例） |
| 2026-07-28 | `BicycleDynamics` / `DdpStateJacobian` / `DdpControlJacobian` / `DdpStateHessianTensor` / `DdpControlHessianTensor` / `DdpMixedHessianTensor` / `DDP_IDX_JERK` / `DDP_IDX_ETA` | 从「待实现：DDP 模块核心接口」表迁移至「已冻结接口清单」并冻结：Milestone 002 经两轮评审收敛（🚨=0 ⚠️=0），正式关闭；雅可比/二阶张量全部公式经独立手工链式推导验证与 DDP.md 2.5 完全一致，全元素 FD 对拍 < 1e-6，benchmark 实测 16.3/43.4/111.2 μs | 首次实现经评审收敛后正式关闭（比照 M001 与 ALM 规划表迁移惯例） |
| 2026-07-28 | `BoxQpSolver` | 从「待实现：DDP 模块核心接口」表迁移至「已冻结接口清单」并冻结：Milestone 003 经两轮评审收敛（🚨=0 ⚠️=0），正式关闭；投影牛顿法/活动集热启动/自由维 Hessian 分解/正则化红线 4 项全部经 14 个单元测试（含 60 组随机 QP 与暴力枚举对照 < 1e-10）+ benchmark（冷启动 1.57/热启动 1.53 分解/QP）验证 | 首次实现经评审收敛后正式关闭（比照 M001/M002 与 ALM 规划表迁移惯例） |
| 2026-07-28 | `DdpCostEvaluator` / `DdpEsdfConstraint` / `DdpCostConfig` / `DdpCostMultiplierState` / `DdpCostInput` / `DdpStageCostDerivatives` / `DdpCostEvaluation` / `DdpEsdfConstraintConfig` / `DdpEsdfPoseCost` / `DdpEsdfCircleConstraint` / `DdpControlHessian` / `DdpControlStateHessian` / `DdpStateHessian` / `DDP_AMPLITUDE_CONSTRAINT_DIM` / `DDP_AMP_*` / `DDP_TERMINAL_CONSTRAINT_DIM` | 从「待实现：DDP 模块核心接口」表迁移至「已冻结接口清单」并冻结：Milestone 004 经两轮评审收敛（🚨=0 ⚠️=0），正式关闭；全部 7 项范围（平滑主项/退火跟踪/换挡代理/幅值 AL/ESDF 双 margin/终点 AL/统一输出接口）经 26 个单元测试（全项 FD < 1e-6 + AL 门控/角度 wrap/退火掩码/双 margin C¹ 衔接/σ_β 门/终点 δω 豁免/dt 因子约定回归）+ benchmark（含 ESDF 1.147 ms/轮、不含 23.8 μs/轮）验证 | 首次实现经评审收敛后正式关闭（比照 M001~M003 与 ALM 规划表迁移惯例） |
| 2026-07-29 | `AlOuterLoop` / `AlOuterLoopConfig` / `AlConstraintSnapshot` / `AlTerminationCheck` | 从「待实现：DDP 模块核心接口」表迁移至「已冻结接口清单」并冻结：Milestone 006 经两轮评审收敛（🚨=0 ⚠️=0），正式关闭；自适应 $\mu^0$ clip 标定（$J_s'/\max(\|c\|^2,\varepsilon_\mu)$）+ Hestenes-Powell 乘子更新 + 终点/幅值分组独立门控（标定轮只标定不增长）+ 退火掩码（首/末 maneuver 豁免/$\gamma=0.5$）+ 联合终止判据（双指标/不等式/缺陷），全部经 15 个外层单元测试验证 | 首次实现经评审收敛后正式关闭（比照 M001~M004 与 ALM 规划表迁移惯例） |
| 2026-07-29 | `ApaDdpSolver` / `ApaDdpSolverConfig` / `ApaDdpReport` / `ApaDdpOuterRecord` / `ApaDdpStageOneResult` | 从「待实现：DDP 模块核心接口」表迁移至「已冻结接口清单」并冻结：Milestone 006 经两轮评审收敛（🚨=0 ⚠️=0），正式关闭；阶段一编排（内层超限不致命/ρ_reg 溢出冷重启一次/δ 投影热启动/anytime 性质）+ 结构化逐轮诊断报告，全部经 8 个编排器测试（含灵魂用例/有用不误融/内层超限不致命/冷重启链路的显式钉住用例）+ 真实数据集冒烟（data3）验证 | 首次实现经评审收敛后正式关闭（比照 M001~M004 与 ALM 规划表迁移惯例） |
| 2026-07-29 | `DdpCostEvaluator` / `DdpCostMultiplierState` / `DdpCostInput` / `DdpStageCostDerivatives` / `DdpGatingPlan`（新增）/ `ApaDdpSolver` / `ApaDdpSolverConfig` / `ApaDdpStageTwoResult`（新增）/ `DdpPostStage` 族（新增） | 阶段二门控精化与后处理的**增量扩展**（既有签名与默认行为全部不变，评审通过后随 Milestone 007 关闭迁移/更新冻结清单）：① 代价层新增 `DdpGatingPlan`（逐节点符号门 ±1/0、接缝索引与查表、逐节点驻留速度帽）；② `DdpCostMultiplierState` 新增六个门控乘子向量（默认空 = 阶段一模式，门控项恒零）；③ `DdpCostInput` 新增可选 `gating_plan` 指针（默认 `nullptr`）；④ `DdpStageCostDerivatives` 新增 `cost_gating` 分量（默认 0，阶段一总代价不变）；⑤ `DdpCostEvaluator::evaluate` 新增门控累加与一致性校验——计划与乘子必须同在场且尺寸匹配，仅一方在场抛 `std::logic_error`（"阶段一禁止启用"的断言防御，阶段一调用路径两者恒不在场），残差公式以静态方法 `SignGateResidual`/`SeamResidual`/`DwellResidual` 开放（驻留帽取 `|v|−cap` 双侧线性形态，活动区梯度模恒 1）；⑥ `ApaDdpSolver` 新增 `solveStageTwo()`（跟踪权重由调用方显式给定、恒定不退火，典型取阶段一末轮退火值；支持 `dual_seed` 对偶热启动——终端 λ/μ 续接并把 μ⁰ 标定下限抬到种子水平）与 `ApaDdpStageTwoResult`，`ApaDdpSolverConfig` 新增阶段二外层上限/门控 μ 调度/门控容差三项，`ApaDdpStageOneResult` 新增 `final_multipliers`（阶段一终态乘子，供对偶热启动）；⑦ 新增 `src/core/DDP/ddp_post_stage.h/.cpp`：`DdpPostStage`（带滞回符号游程分析、复用 `util::topology_cleaner` 的拓扑修剪红线封装、门控计划构建、阶段一解→阶段二网格热启动映射、逐接缝驻留插入时间拉伸、六项校验清单与回退出口）及配套配置/诊断结构体；⑧ `DdpReferenceBuilder` 新增只读访问器 `sampleDist()`（退化诊断需要与 builder 同源的重采样间距，Round 1 评审意见处理） | 后处理与阶段二门控精化（DDP.md 2.6 节六步）的落地需要：三类门控 AL 约束（段内符号门/接缝零速等式/驻留速度帽）必须折叠进代价求值层的逐阶段导数才能被内层 MS-iLQR 消费；扩展全部采取"新增可选字段、默认关闭"形式，阶段一求解路径行为逐位不变 |
| 2026-07-29 | `ApaDdpStageTwoResult` / `DdpGatingPlan` / `DdpCostMultiplierState`（门控扩展）/ `DdpPostStage` 族 | 从「待实现：DDP 模块核心接口」表迁移至「已冻结接口清单」并冻结：Milestone 007 经两轮评审收敛（🚨=0 ⚠️=0），正式关闭；阶段二门控重解 + 六步后处理全部经 28 个单元测试（含 T_resteer 两分支解析对拍、符号游程滞回、修剪红线四例、门控方向双向量测、代价层 FD 对拍/阶段一隔离、门控 μ 充分下降、对偶热启动对照、驻留插入时间拉伸/逐点断言、端到端融化/保留换挡/回退路径）验证 | 首次实现经评审收敛后正式关闭（比照 M001~M006 与 ALM 规划表迁移惯例） |
| 2026-07-29 | `PostProcessor` / `PostProcessorResult` / `DdpConfig`（新增）/ `LoadDdpConfigOverrides`（新增）/ `DDPPlanningScene`（新增）/ `PlanningScene::LoadFromFile` / `data/ddp_config.json`（新增） | 新增 DDP 路径编排入口 `PostProcessor::optimizeDdp()`（Path → DdpReferenceBuilder → ApaDdpSolver 阶段一全局软化求解 → DdpPostStage 后处理与阶段二门控精化 → 校验/回退），与 NMPC/ALM 路径并列互不干扰；`PostProcessorResult` 新增 `ddp_traj` 字段（成功时承载含驻留与时间戳的最终轨迹，失败/回退时为空）；新增 `DdpConfig`（继承 `Config` 基类，与 `AlmConfig` 同模式，聚合 reference/solver/esdf/post_stage 四组子配置）——状态幅值边界（v_max/a_max/δ_max/ω_max）以 reference 子配置为唯一权威来源、η_max 以 inner.steer_accel_max 为唯一权威来源，构造与 JSON 加载时经 `synchronizeAmplitudeBounds()` 同步进 cost/post_stage 同源字段（2026-07-25 Config 双源缺口教训的前置防御），`PostProcessor::optimizeDdp` 在局部配置副本上再同步一次以防御直接字段改写；新增 JSON 加载自由函数 `LoadDdpConfigOverrides`（仅覆盖显式出现的字段，未出现的保持默认值，cost 节不接收幅值键、post_stage 节不接收 ω/η 上限键——它们在 JSON 层同样单一来源）；新增 `src/scene/ddp_planning_scene.h/.cpp`（效仿 `ALMPlanningScene` 的委托构造与类型化观察指针模式）；`PlanningScene::LoadFromFile` 路由扩展识别 `"algorithm": "ddp"`；新增 `data/ddp_config.json`（沿用"每算法一个配置详情 JSON"约定，显式列出 DDP.md 2.5 节参数表默认值作为调参模板，`outer_row_num=2` 与 DDP 真实数据集冒烟验证的碰撞模型一致）；`PostProcessor`/`DdpConfig` 同步登记/更新进「已冻结接口清单」 | DDP 求解链路接入仓库编排与场景框架，形成与 NMPC/ALM 并列的第三条完整算法路径（设计文档 [docs/DDP.md](DDP.md) 2.5/2.6 节，接入模式对齐 2026-07-21 ALM 接入惯例）；幅值边界单一权威来源 + 自动同步是 Config 基类字段双源缺口历史教训的前置防御，避免 JSON/代码两侧各自改写导致的跨阶段数值不自洽 |

## 待办：NMPC 安全机制重构相关的计划中接口变更

| 计划变更 | 接口 | 原因 |
|---|---|---|
| `data6.json` 静态走廊路径首迭代 HPIPM `UNKNOWN_ERROR`（状态码 5）根因排查 | `NmpcSolver`、`third_party/StcSQP::HPIPMQPSolver` | 已排除 ng_max 维度不一致、终端约束、信赖域约束三类假设（三者关闭后仍复现同一 `UNKNOWN_ERROR`），根因仍未定位，疑似 HPIPM IPM 内部数值条件问题；按 debug-circuit-breaker 约定暂停继续排查，留待后续专项调试（未解决，见 `docs/known-limitations.md`） |

## 待办：预处理管线相关的计划中接口变更

| 计划变更 | 接口 | 原因 |
|---|---|---|
| ~~碰撞安全裕度参数统一~~ | ~~`BSplineSmootherConfig`、`StaticCorridorBuilderConfig`~~ | ✅ 已完成 |
| ~~静态走廊信赖域约束注入~~ | ~~`NmpcSolverConfig`、`NmpcSolver`~~ | ✅ 已完成。`max_theta_deviation_from_ref` 字段已新增（默认 0.0=关闭）；`ThetaTrustRegionConstraint` 约束类已实现并通过单元测试；`NmpcSolver::optimize()` 已接线 |
| ~~HPIPM 层静态走廊约束注入~~ | ~~`NmpcSolver`~~ | ✅ 已完成。`StaticCorridorLinearConstraint` 约束类已实现并通过单元测试；`NmpcSolver::optimize()` 在 `static_corridor_C/d` 提供时自动注入，否则兜底使用 `esdf_penalty_weight` 动态 ESDF 软代价 |
| ~~`main.cpp` 生产入口接入 `PreprocessingPipeline`~~ | ~~`src/main.cpp`、`NmpcSolver`~~ | ✅ 已完成：`main.cpp` 已切换为 `Path → PreprocessingPipeline → PreprocessingToOcpConverter → NmpcSolver` 完整链路，并实现了"NMPC 失败但预处理成功时回退到预处理轨迹"的四态判定 |
| ~~StcSQP 热循环基础设施优化（`-march=native` + `CostTerm` 组合求值 + `assembleQP` 并行化）~~ | ~~`stc_SQP::CostTerm`、`stc_SQP::SQPSolver`、`third_party/StcSQP/CMakeLists.txt`~~ | ✅ 已完成。三处内部实现优化均已落地：`CostTerm` 新增 `evaluateGradientAndHessian()`/`clone()`，`CircleFootprintEsdfPenaltyCost`/`CompositeCost`/`QuadraticTrackingCost` 实现对应方法；`SQPSolver` 新增 `thread_cost_clones_` 与 per-thread `CostScratch`；`stc_SQP_core_lib` 在 x86_64 平台启用 `-march=native` |
| HPIPM IPM 跨迭代热启动 | `stc_SQP::HPIPMQPSolver`、`stc_SQP::SQPSolver` | ✅ 已完成。已实现 primal-only 跨迭代热启动与 `use_qp_warm_start` 开关；benchmark 显示在 `BM_Data3RealScenario_MultiSegmentBicycleCorridor` 场景下总 IPM 迭代数与总耗时均未出现可观测收益，因此默认关闭，作为可选实验开关保留 |
| ~~`Constraint`/`CostTerm` 虚函数多态迁移到 `std::variant`~~ | ~~`stc_SQP::StageSegment`、`stc_SQP::Constraint`、`stc_SQP::CostTerm`~~ | ✅ 已评估不采纳。Release 基准 `std::variant` 802ms vs 虚函数 726ms 处于同一量级无收益；结合 closed-set 维护成本，最终决定不合入，保留原有虚函数机制 |
| ~~`lbfgs_max_iterations`/`lbfgs_max_linesearch` 默认值收紧~~ | ~~`BSplineSmootherConfig`~~ | ✅ 已完成。`lbfgs_max_iterations` 100→80，`lbfgs_max_linesearch` 100→20，详见上方变更记录 |
| ~~预处理管线可视化诊断入口~~ | ~~`Visualizer`（新增 `plotPipelineDiagnostics`）、`DetailSeriesData`（扩展 `v`/`a`/`delta`/`delta_dot`/`delta_t`/`slack_true`/`slack_calc` 字段）、`PreprocessingPipelineConfig`（新增 `enable_debug_output` 开关）、`PreprocessingPipelineResult`（新增 `debug_maneuver_outputs` 容器）~~ | ✅ 已完成。`plotPipelineDiagnostics` 已按 2x3 子图渲染；`slack_true` 与 `slack_calc` 在真实数据集上数值吻合；单元测试覆盖开关行为、空输入死区保护、双 Y 轴量程、OBB 包络点数、slack 对比 |

## 待办：NMPC 算法最终重构相关的计划中接口变更

> 本表登记"NMPC 算法最终重构"系列尚未实现的接口变更意图；具体类名/方法签名由 Dev Agent 在实施时最终确定，
> 落地后需将本表对应行改为完成态（比照上一张表的 ✅ 划线惯例），并在下方"变更记录"补充正式条目，不得只在本表登记而不同步。

| 计划变更 | 接口 | 原因 |
|---|---|---|
| ~~`NmpcSolver` 新增接受预装配 OCP/初始猜测的扩展点~~ | ~~`NmpcSolver`（`src/core/NMPC/nmpc_solver.h`）~~ | ✅ 已完成：新增 `optimize(const MultiStageOCP&, const Trajectory&, const ESDFMap&)` 重载，约束注入与求解逻辑下沉到 `solveOcp()` 共享 |
| ~~`PathToOcpConverter` 职责边界复核（拆分"OCP 结构装配"与"初始猜测生成"）~~ | ~~`PathToOcpConverter`（`src/core/NMPC/path_to_ocp_converter.h`）~~ | ✅ 已完成：新增 `SegmentProfile` + `computeSegmentProfiles()` / `generateInitialGuess()` / `buildOcp()`，使 OCP 结构装配与初始猜测生成解耦 |
| ~~新增"预处理管线输出 → OCP"转换路径，消费非均匀 `delta_t`~~ | ~~新增类型（具体命名由 Dev Agent 确定），`stc_SQP::StageSegment::dt_array`~~ | ✅ 已完成：新增 `PreprocessingToOcpConverter`，把 `PreprocessingPipelineResult` 的 `z_ref`/`delta_t`/`c_matrix`/`d_vector` 直接作为 Warm Start，通过 `StageSegment::dt_array` 接入非均匀步长 |
| ~~`main.cpp`/`NmpcSolver` 的"NMPC 失败回退预处理轨迹"三/四态判定~~ | ~~`src/main.cpp`、`NmpcSolver`~~ | ✅ 已完成：`main.cpp` 中实现"NMPC 收敛"/"NMPC 未收敛但仍返回最新迭代"/"回退到预处理轨迹"/"整体失败"四态，日志与 `OptimizeResponse.message` 明确区分 |
| ~~自适应间隔重试机制（"拉长间隔重试、用完必须恢复默认值"）~~ | ~~`PreprocessingPipelineConfig`/`BSplineSmootherConfig::dense_step_dist` 或 `AdaptiveResamplerConfig::nominal_step_s`，新增 `PostProcessor`/`AdaptiveRetryConfig`~~ | ✅ 已完成：实测选定 `AdaptiveResamplerConfig::nominal_step_s` 作为重试调节对象（`dense_step_dist` 放大易触发 `dt_array` 非正异常）；`PostProcessor::optimize()` 内部使用局部配置副本，调用方默认配置保持不变；单元测试 `PostProcessorTest.DoesNotLeakConfigAfterRetry` 直接验证 |

## 待实现：ALM 模块核心接口（新模块，原开发规划）

> 与 [docs/architecture.md](architecture.md) 3.7 节对应：`src/core/ALM/` 是与 `src/core/NMPC/` 并列的第二条
> 后处理算法路径。本节原表登记各 Milestone 拆分阶段**尚未实现**的接口设计意图与目标头文件路径；
> 截至 `PostProcessor` 接入 ALM 路径的扩展点落地，全部规划接口均已实现并经评审
> 迁移至上方"已冻结接口清单"（见变更记录 2026-07-20/2026-07-21 各条目），本表不再有待办行，
> 保留本节标题以维持历史文档链接有效。

## 待办：ALM 收尾与独立化相关的计划中接口变更

> 对应一次全局质量校验与后续代码复核发现的问题，登记四个后续 Milestone
> 计划中的接口变更意图；具体类名/方法签名由 Dev Agent 在实施时最终确定，
> 落地后需将本表对应行改为完成态（比照既有表格的 ✅ 划线惯例），并在下方"变更记录"补充正式条目。

| 计划变更 | 接口 | 原因 |
|---|---|---|
| ~~`AlmManeuverMelter` 的 PIVOT 处理取消位置冻结~~ | ~~`AlmManeuverMelter`（`src/core/ALM/alm_maneuver_melter.h`）~~ | ✅ 已完成：`PIVOT` 已收窄为纯分类标签 + `Direction::PIVOT` 方向标签，不再改写任何采样点字段；详见上方变更记录 2026-07-22 条目 |
| ~~从 `AlmManeuverMelter::sampleToManeuvers` 中拆出独立可复用的"MincoTrajectory → 采样 Path"离散化工具~~ | ~~新增类型/方法（具体形态由 Dev Agent 确定，可能是独立函数或 `AlmManeuverMelter` 的公开方法），`src/core/ALM/`~~ | ✅ 已完成：形态定为独立自由函数 `SampleMincoTrajectory` + `CheckMincoSampleStructure`（`src/core/ALM/alm_trajectory_sampler.h`），`AlmManeuverMelter::meltAndPrune` 改为调用该工具、不重复实现；详见上方变更记录 2026-07-22 条目 |
| ~~`main.cpp` 生产入口不再依赖 `NMPCPlanningScene`~~ | ~~`src/main.cpp`~~ | ✅ 已完成：`main.cpp` 只构造并执行 `ALMPlanningScene`，对比图改为 ALM 自身"预处理粗优化轨迹（红，优化前） vs 主优化+机动融化后轨迹（绿，优化后）"；详见上方变更记录 2026-07-22 条目 |
| ~~`TrajectoryValidationConfig`/`TrajectoryValidationResult` 新增运动学可行性校验字段~~ | ~~`Trajectory`（`src/util/trajectory.h`）~~ | ✅ 已完成：新增梯形配点残差运动学门（`kinematic_feasible` + 四项最大残差 + `kinematic_detail`），Config 新增 `max_kinematic_dt=0.5` 与四项阈值（位置 0.02m/航向 3.0°/速度 0.05m·s⁻¹/转角 0.02rad），标定依据与实测分布见上方变更记录 2026-07-22 条目 |

## 待实现：DDP 模块核心接口（新模块，开发规划）

> 与 [docs/architecture.md](architecture.md) 3.8 节对应：`src/core/DDP/` 是与 `src/core/NMPC/`、`src/core/ALM/` 并列的
> 第三条后处理算法路径，完整数学设计见 [docs/DDP.md](DDP.md)，Milestone 拆分见 [docs/milestones.md](milestones.md)（001~009）。
> 本表登记**尚未实现**接口的设计意图与目标头文件路径；具体类名/方法签名由 Dev Agent 在实施时最终确定，
> 落地并经评审收敛后应将对应行迁移至上方"已冻结接口清单"（比照 ALM 规划表 2026-07-20/21 的迁移惯例），
> 并在上方"变更记录"补充正式条目。

| 规划接口 | 目标头文件 | 设计意图（对应 DDP.md 章节） |
|---|---|---|
| ~~`DdpReferenceBuilder`（含参考/初值/打靶节点输出结构体）~~ | ~~`src/core/DDP/ddp_reference_builder.h`~~ | ✅ 已完成：已迁移至上方「已冻结接口清单」（Milestone 001 关闭） |
| ~~`BicycleDynamics`（七维状态链）~~ | ~~`src/core/DDP/bicycle_dynamics.h`~~ | ✅ 已完成：已迁移至上方「已冻结接口清单」（Milestone 002 关闭） |
| ~~`BoxQpSolver`~~ | ~~`src/core/DDP/box_qp.h`~~ | ✅ 已完成：已迁移至上方「已冻结接口清单」（Milestone 003 关闭） |
| ~~`DdpCostEvaluator`（含 AL 增广项）~~ | ~~`src/core/DDP/ddp_cost.h`~~ | ✅ 已完成：已迁移至上方「已冻结接口清单」（Milestone 004 关闭） |
| ~~`DdpEsdfConstraint`~~ | ~~`src/core/DDP/esdf_constraint.h`~~ | ✅ 已完成：已迁移至上方「已冻结接口清单」（Milestone 004 关闭） |
| `MsIlqrSolver`（含 Workspace/对齐分配器容器） | `src/core/DDP/ms_ilqr.h` | 缺陷感知 Riccati 回推（右端索引约定 $d[i]$）+ 每步 box-QP；`linearRollout()` 与 `nonlinearRollout(alpha)` 两套独立接口，$EC_1/EC_2$ 成员缓存；自适应 $\mu_m$ merit + Armijo；LM 正则化调度（增 $\rho_{reg}$ 必重建全部 QP 分解）；可选段间惩罚 $Q_d$（1.4/2.5 节） |
| ~~`AlOuterLoop`~~ | ~~`src/core/DDP/al_outer_loop.h`~~ | ✅ 已完成：已迁移至上方「已冻结接口清单」（Milestone 006 关闭） |
| ~~`ApaDdpSolver`~~ | ~~`src/core/DDP/apa_ddp_solver.h`~~ | ✅ 已完成：已迁移至上方「已冻结接口清单」（Milestone 006 关闭） |
| ~~`DdpPostStage`（符号游程/修剪/驻留/校验）~~ | ~~`src/core/DDP/ddp_post_stage.h`~~ | ✅ 已完成：已迁移至上方「已冻结接口清单」（Milestone 007 关闭） |
| ~~`DdpConfig` / `PostProcessor::optimizeDdp()`~~ | ~~`src/util/config.h` / `src/core/post_processor.h`~~ | ✅ 已完成：已迁移至上方「已冻结接口清单」（Milestone 008）；`DdpConfig` 实际落地于 `src/core/post_processor.h`（与 `AlmConfig` 同文件同模式，避免 util → core 反向分层依赖） |
| ~~`DDPPlanningScene` / `data/ddp_config.json`~~ | ~~`src/scene/ddp_planning_scene.h`~~ | ✅ 已完成：场景类与配置文件均已落地（Milestone 008）；`PlanningScene::LoadFromFile` 路由已扩展识别 `"algorithm": "ddp"` |




